/**
 * Thin alias kept for existing imports. There is ONE public site header:
 * ConsumerNav (public items for visitors, Hamilton items for Pro users).
 */
export { ConsumerNav as CustomerNav } from "./consumer-nav";
