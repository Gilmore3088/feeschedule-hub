/**
 * Thin alias kept for existing imports. The single mobile drawer lives in
 * ConsumerMobileNav and accepts the same props ({ isLoggedIn, isPro }).
 */
export { ConsumerMobileNav as MobileNav } from "./consumer-mobile-nav";
