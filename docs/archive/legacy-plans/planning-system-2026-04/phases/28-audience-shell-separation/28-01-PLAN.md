---
phase: 28-audience-shell-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/consumer-nav.tsx
  - src/components/consumer-mobile-nav.tsx
  - src/lib/personalization.ts
  - src/lib/personalization.test.ts
  - src/app/(public)/layout.tsx
autonomous: true
requirements:
  - SHELL-01
  - SHELL-03

must_haves:
  truths:
    - "Consumer visitors on (public) routes see ConsumerNav with public links (Find Your Institution, Fee Benchmarks, Research, Guides) and Sign In CTA"
    - "derivePersonalizationContext(user) returns institutionName, fedDistrictLabel, assetTier, peerGroupLabel from user profile fields without any DB call"
    - "ConsumerNav is a dedicated component with no conditional pro/consumer branching"
  artifacts:
    - path: "src/components/consumer-nav.tsx"
      provides: "Public-facing nav component"
      exports: ["ConsumerNav"]
    - path: "src/components/consumer-mobile-nav.tsx"
      provides: "Consumer mobile nav drawer"
      exports: ["ConsumerMobileNav"]
    - path: "src/lib/personalization.ts"
      provides: "Personalization context derivation"
      exports: ["derivePersonalizationContext", "PersonalizationContext"]
    - path: "src/lib/personalization.test.ts"
      provides: "Tests for personalization service"
  key_links:
    - from: "src/app/(public)/layout.tsx"
      to: "src/components/consumer-nav.tsx"
      via: "import ConsumerNav"
      pattern: "import.*ConsumerNav.*from"
    - from: "src/lib/personalization.ts"
      to: "src/lib/fed-districts.ts"
      via: "STATE_TO_DISTRICT + DISTRICT_NAMES"
      pattern: "STATE_TO_DISTRICT|DISTRICT_NAMES"
---

<objective>
Create the ConsumerNav component and personalization service that form the consumer-side shell and the data layer for the pro-side shell.

Purpose: Separate consumer navigation from the shared CustomerNav, and build the personalization service that ProNav (Plan 02) will consume. This is the foundation for all Phase 29-32 audience-specific work.

Output: ConsumerNav component wired into (public) layout, personalization service with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-audience-shell-separation/28-CONTEXT.md

@src/components/customer-nav.tsx
@src/components/mobile-nav.tsx
@src/app/(public)/layout.tsx
@src/lib/auth.ts
@src/lib/fed-districts.ts
@src/lib/access.ts

<interfaces>
<!-- Key types the executor needs -->

From src/lib/auth.ts:
```typescript
export interface User {
  id: number;
  username: string;
  display_name: string;
  role: "viewer" | "analyst" | "admin" | "premium";
  email: string | null;
  stripe_customer_id: string | null;
  subscription_status: "none" | "active" | "past_due" | "canceled";
  institution_name: string | null;
  institution_type: string | null;
  asset_tier: string | null;
  state_code: string | null;
  job_role: string | null;
  interests: string | null;
}
export async function getCurrentUser(): Promise<User | null>;
```

From src/lib/fed-districts.ts:
```typescript
export const STATE_TO_DISTRICT: Record<string, number>;
export const DISTRICT_NAMES: Record<number, string>;
export const FDIC_TIER_LABELS: Record<string, string>;
```

From src/lib/access.ts:
```typescript
export function canAccessPremium(user: User | null): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create personalization service with tests</name>
  <files>src/lib/personalization.ts, src/lib/personalization.test.ts</files>
  <read_first>
    - src/lib/auth.ts (User interface -- institution_name, institution_type, asset_tier, state_code fields)
    - src/lib/fed-districts.ts (STATE_TO_DISTRICT mapping, DISTRICT_NAMES, FDIC_TIER_LABELS)
  </read_first>
  <behavior>
    - derivePersonalizationContext({ institution_name: "First National", state_code: "NY", asset_tier: "community", institution_type: "bank" }) returns { institutionName: "First National", fedDistrictLabel: "New York", assetTier: "Community ($100M-$1B)", peerGroupLabel: "Community Banks ($100M-$1B)" }
    - derivePersonalizationContext with state_code: null returns fedDistrictLabel: null
    - derivePersonalizationContext with asset_tier: null returns assetTier: null, peerGroupLabel with just institution_type
    - derivePersonalizationContext with institution_type: null returns peerGroupLabel: null
    - derivePersonalizationContext with institution_type: "credit_union" returns "Credit Unions" in peerGroupLabel
    - Function is pure (no DB calls, no async, no side effects)
  </behavior>
  <action>
    Per D-06, D-07, D-08: Create `src/lib/personalization.ts` with:

    1. Export `PersonalizationContext` interface:
       ```typescript
       export interface PersonalizationContext {
         institutionName: string | null;
         fedDistrictLabel: string | null;
         assetTier: string | null;
         peerGroupLabel: string | null;
       }
       ```

    2. Export `derivePersonalizationContext(user: User): PersonalizationContext` as a pure function:
       - `institutionName`: `user.institution_name` (pass through)
       - `fedDistrictLabel`: Look up `STATE_TO_DISTRICT[user.state_code]` to get district number, then `DISTRICT_NAMES[districtNum]`. Return null if state_code is null or not found.
       - `assetTier`: Look up `FDIC_TIER_LABELS[user.asset_tier]`. Return null if asset_tier is null or not found.
       - `peerGroupLabel`: Combine institution_type and asset_tier. Format: "Community Banks ($100M-$1B)" or "Credit Unions ($100M-$1B)". institution_type "bank" -> "Banks", "credit_union" -> "Credit Unions". If institution_type is null, return null. If asset_tier is null, return just the type label (e.g., "Banks").

    NOTE: `getDistrictForState()` does not exist in fed-districts.ts. Use `STATE_TO_DISTRICT[stateCode]` directly, which returns a district number, then look up `DISTRICT_NAMES[districtNum]`.

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && npx vitest run src/lib/personalization.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - personalization.ts exports PersonalizationContext and derivePersonalizationContext
    - All 6+ test cases pass covering: full profile, null state_code, null asset_tier, null institution_type, credit_union type, unknown state_code
    - Function is synchronous (not async), takes User, returns PersonalizationContext
  </acceptance_criteria>
  <done>All personalization tests pass. Function correctly derives all four context fields from User profile without DB calls.</done>
</task>

<task type="auto">
  <name>Task 2: Create ConsumerNav and wire into public layout</name>
  <files>src/components/consumer-nav.tsx, src/components/consumer-mobile-nav.tsx, src/app/(public)/layout.tsx</files>
  <read_first>
    - src/components/customer-nav.tsx (current shared nav -- extract consumer path)
    - src/components/mobile-nav.tsx (current mobile nav -- extract consumer path)
    - src/app/(public)/layout.tsx (current layout importing CustomerNav)
    - src/app/admin/admin-nav.tsx (pattern reference for client nav with usePathname)
  </read_first>
  <action>
    Per D-01, D-03, D-09:

    1. Create `src/components/consumer-nav.tsx`:
       - This is an async server component (like current CustomerNav) -- calls `getCurrentUser()` to check login state
       - Nav items (hardcoded, no conditional branching):
         - "Find Your Institution" -> /institutions
         - "Fee Benchmarks" -> /fees
         - "Research" -> /research
         - "Guides" -> /guides
         - If not logged in: "Pricing" -> /subscribe
       - Right side: SearchTrigger + Sign In link (if not logged in) or Account link with avatar (if logged in)
       - Include "Get Pro Access" CTA button when user is not logged in or not premium: `<Link href="/subscribe" className="...">Get Pro Access</Link>` styled as a small filled button with the brand red (#C44B2E)
       - Mobile: import ConsumerMobileNav (not the shared MobileNav)
       - Copy the visual structure from current CustomerNav but remove ALL pro-related logic (no isPro check, no pro nav items, no Pro badge)
       - Keep the same sticky header, logo, Newsreader font, warm palette (#FAF7F2, #E8DFD1, #1A1815, #7A7062, #C44B2E)

    2. Create `src/components/consumer-mobile-nav.tsx`:
       - Client component ("use client") with useState for open/close, usePathname for active state
       - Hardcoded consumer nav items only (same 4-5 links as desktop)
       - Same drawer pattern as current mobile-nav.tsx but stripped of isPro branching
       - Include Sign In / Account link in footer section

    3. Update `src/app/(public)/layout.tsx`:
       - Change import from `CustomerNav` to `ConsumerNav` from `@/components/consumer-nav`
       - Replace `<CustomerNav />` with `<ConsumerNav />`
       - Keep CustomerFooter and SearchModal as-is

    Do NOT delete or modify customer-nav.tsx or mobile-nav.tsx -- they are still used by pro/layout.tsx until Plan 02 replaces them.
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ConsumerNav renders public nav items only (no pro links, no isPro branching)
    - ConsumerMobileNav is a dedicated client component with consumer-only links
    - (public)/layout.tsx imports ConsumerNav (not CustomerNav)
    - TypeScript compiles without errors
    - customer-nav.tsx and mobile-nav.tsx still exist unchanged (used by pro layout until Plan 02)
  </acceptance_criteria>
  <done>Consumer visitors on any (public) route see ConsumerNav with public links and Sign In / Get Pro Access CTAs. No conditional branching between consumer and pro in the nav component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer nav -> auth | Nav calls getCurrentUser() which touches cookies -- already handled by existing auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | personalization.ts | accept | Pure function on already-authenticated User object; no new data exposure. User data already loaded by getCurrentUser(). |
| T-28-02 | Spoofing | consumer-nav.tsx | accept | Same auth pattern as existing CustomerNav -- getCurrentUser() with HMAC-signed session cookie. No new attack surface. |
</threat_model>

<verification>
- `npx vitest run src/lib/personalization.test.ts` -- all personalization tests pass
- `npx tsc --noEmit` -- no TypeScript errors
- Consumer layout renders ConsumerNav (verify import in layout.tsx)
</verification>

<success_criteria>
- ConsumerNav component exists and is wired into (public)/layout.tsx
- Personalization service passes all tests with correct district, tier, and peer group derivation
- No changes to admin layout or existing customer-nav.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/28-audience-shell-separation/28-01-SUMMARY.md`
</output>
