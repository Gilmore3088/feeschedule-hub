---
phase: 17-hamilton-chat-unified-research-interface
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - src/app/admin/hamilton/page.tsx
  - src/app/admin/hamilton/layout.tsx
  - src/app/admin/admin-nav.tsx
  - src/app/admin/research/page.tsx
  - src/app/admin/research/[agentId]/page.tsx
  - src/app/admin/scout/page.tsx
autonomous: false
requirements:
  - D-02
  - D-11
  - D-12
  - D-13
  - D-14

must_haves:
  truths:
    - "Visiting /admin/hamilton lands on the Chat tab by default"
    - "Chat tab and Reports tab are visible — switching between them works"
    - "Reports tab shows the existing report management UI (unchanged)"
    - "Visiting /admin/research redirects to /admin/hamilton"
    - "Visiting /admin/scout redirects to /admin/hamilton"
    - "Admin nav shows Hamilton as a single entry — Research Hub and Scout are removed from nav"
    - "Hamilton nav item links to /admin/hamilton (which defaults to Chat tab)"
  artifacts:
    - path: "src/app/admin/hamilton/layout.tsx"
      provides: "Tab layout wrapper with Chat + Reports tabs (URL-based tab switching)"
      exports: ["default (HamiltonLayout)"]
    - path: "src/app/admin/hamilton/page.tsx"
      provides: "Redirects to /admin/hamilton/chat (Chat is the default tab per D-02)"
      exports: ["default (redirect)"]
    - path: "src/app/admin/admin-nav.tsx"
      provides: "Updated nav: Hamilton entry pointing to /admin/hamilton; Research + Scout removed"
    - path: "src/app/admin/research/page.tsx"
      provides: "Permanent redirect (308) to /admin/hamilton"
    - path: "src/app/admin/scout/page.tsx"
      provides: "Permanent redirect (308) to /admin/hamilton"
  key_links:
    - from: "src/app/admin/hamilton/layout.tsx"
      to: "src/app/admin/hamilton/chat/page.tsx"
      via: "Tab link href=/admin/hamilton/chat renders children"
      pattern: "hamilton/chat"
    - from: "src/app/admin/hamilton/layout.tsx"
      to: "src/app/admin/hamilton/reports"
      via: "Tab link href=/admin/hamilton/reports"
      pattern: "hamilton/reports"
---

<objective>
Wire the two-tab layout at `/admin/hamilton`, make Chat the default landing, and consolidate the nav by removing Research Hub and Scout as separate destinations (per D-11, D-12, D-13, D-14). The existing Reports tab content is preserved intact — it just moves to `/admin/hamilton/reports`.

Purpose: Complete the agent consolidation. The user now has one place to go: Hamilton. Research Hub and Scout redirect there. The nav is simplified.

Output:
- Hamilton layout with Chat + Reports tabs (URL-driven tab state)
- `/admin/hamilton` page redirects to `/admin/hamilton/chat`
- Reports content moves to `/admin/hamilton/reports/` (or stays at current path with tab overlay)
- Research + Scout pages become 308 redirects
- Nav updated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-hamilton-chat-unified-research-interface/17-CONTEXT.md
@.planning/phases/17-hamilton-chat-unified-research-interface/17-01-SUMMARY.md
@.planning/phases/17-hamilton-chat-unified-research-interface/17-02-SUMMARY.md

@src/app/admin/hamilton/page.tsx
@src/app/admin/admin-nav.tsx
@src/app/admin/hamilton/report-controls.tsx
@src/app/admin/research/page.tsx
@src/app/admin/scout/page.tsx
</context>

<interfaces>
<!-- Key interfaces and patterns -->

Next.js redirect (App Router):
```typescript
// For server component permanent redirect:
import { redirect } from "next/navigation";
export default function Page() {
  redirect("/admin/hamilton/chat"); // 307 by default; use permanentRedirect() for 308
}
// For 308 permanent:
import { permanentRedirect } from "next/navigation";
```

Tab layout pattern (URL-based, no client state):
```typescript
// layout.tsx reads pathname to highlight active tab
// Each tab is just a link — Next.js routing handles the rest
// Use usePathname() in a client sub-component for active state
// OR: pass segment via server component props (simpler — use server layout with Link components)
```

Existing admin-nav.tsx structure (from file read):
```typescript
const NAV_GROUPS: { label?: string; items: NavItem[] }[] = [
  // Dashboard group (no label)
  // Benchmarks group
  // Content group (has Research Hub, Scout)
  // etc.
];
// Hamilton entry needs to be in one group; Research Hub + Scout entries must be removed
```

Existing hamilton/page.tsx:
// Currently renders the full Reports page UI (jobs table + generation controls)
// After this plan: becomes a redirect to /admin/hamilton/chat
// Reports content moves to /admin/hamilton/reports/page.tsx (or stays embedded in a reports tab)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tab layout + reports sub-route + page redirect</name>
  <files>
    src/app/admin/hamilton/layout.tsx
    src/app/admin/hamilton/page.tsx
    src/app/admin/hamilton/reports/page.tsx
  </files>
  <read_first>
    - src/app/admin/hamilton/page.tsx (full file — current Reports page content moves to reports/page.tsx)
    - src/app/admin/hamilton/report-controls.tsx (import path must remain stable)
    - src/app/admin/hamilton/actions.ts (server actions import path must remain stable)
  </read_first>
  <action>
    Step 1 — Create `src/app/admin/hamilton/layout.tsx` (server component):

    This layout renders:
    - A two-tab strip at the top: "Chat" linking to `/admin/hamilton/chat` and "Reports" linking to `/admin/hamilton/reports`
    - The tab strip uses the admin design system: `border-b border-gray-100`, tab links with `text-sm font-medium`, active tab highlighted with `border-b-2 border-gray-900 text-gray-900`, inactive tab `text-gray-400 hover:text-gray-700`
    - Below the tabs: `{children}` (the current page's content)
    - The layout itself does NOT re-render the existing `page.tsx` job table — that content is moving to `reports/page.tsx`

    To detect the active tab without `usePathname()` in a server layout:
    - Create a tiny `"use client"` sub-component `HamiltonTabs` (inline in layout.tsx or as separate file) that imports `usePathname` and renders the two tab links with correct active styling

    The tab strip should sit INSIDE the admin content area (not replacing the global admin nav), matching the visual pattern of other multi-tab admin pages.

    Step 2 — Update `src/app/admin/hamilton/page.tsx`:
    - Replace entire contents with:
      ```typescript
      import { permanentRedirect } from "next/navigation";
      export default function HamiltonIndexPage() {
        permanentRedirect("/admin/hamilton/chat");
      }
      ```
    - This makes Chat the default landing per D-02.

    Step 3 — Create `src/app/admin/hamilton/reports/page.tsx`:
    - Move the ENTIRE existing content of the original `src/app/admin/hamilton/page.tsx` here (copy verbatim before the redirect overwrites it)
    - The report generation controls, jobs table, filters — all move here unchanged
    - Update the `metadata` title to `"Hamilton — Reports — Bank Fee Index Admin"`
    - The imports for `ReportControls`, `publishReport`, `retryReport`, `cancelReport`, `cancelAllPending`, `timeAgo`, report types — all stay the same (paths are unchanged since they are relative to the hamilton directory)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "hamilton/layout|hamilton/page|hamilton/reports" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` renders two tabs: Chat → `/admin/hamilton/chat`, Reports → `/admin/hamilton/reports`
    - `page.tsx` calls `permanentRedirect("/admin/hamilton/chat")`
    - `reports/page.tsx` contains full report management UI (copied from original page.tsx)
    - No TypeScript errors in any of the three files
    - All import paths in reports/page.tsx remain valid (same directory as original)
  </acceptance_criteria>
  <done>Tab layout exists; visiting /admin/hamilton redirects to /admin/hamilton/chat; Reports content lives at /admin/hamilton/reports</done>
</task>

<task type="auto">
  <name>Task 2: Nav consolidation + Research/Scout redirects</name>
  <files>
    src/app/admin/admin-nav.tsx
    src/app/admin/research/page.tsx
    src/app/admin/research/[agentId]/page.tsx
    src/app/admin/scout/page.tsx
  </files>
  <read_first>
    - src/app/admin/admin-nav.tsx (full file — identify Research Hub + Scout items and their group)
    - src/app/admin/research/page.tsx (current content — to be replaced with redirect)
    - src/app/admin/scout/page.tsx (current content — to be replaced with redirect)
  </read_first>
  <action>
    Step 1 — Update `src/app/admin/admin-nav.tsx`:

    Locate the Hamilton nav item (if it exists — may already be present since the directory exists). If present, ensure it:
    - `href: "/admin/hamilton"` (not a sub-path — the layout handles the redirect to Chat)
    - `label: "Hamilton"`
    - Uses an appropriate icon (brain/analyst icon or keep existing)
    - Has `exact: false` so it highlights for all `/admin/hamilton/*` sub-routes (use `pathname.startsWith(item.href)` in the active check — read how the current active logic works first)

    Remove Research Hub nav item (if present) — it redirects to Hamilton now.
    Remove Scout nav item (if present) — it redirects to Hamilton now.

    If Hamilton nav item does NOT exist yet, add it in the appropriate group (likely "Content" or standalone). Place it after Dashboard/Benchmarks groups.

    The active highlight logic in admin-nav.tsx uses `exact` flag — for Hamilton, ensure `exact: false` (or omit `exact`) so the item stays highlighted when on `/admin/hamilton/chat` and `/admin/hamilton/reports`.

    Step 2 — Replace `src/app/admin/research/page.tsx`:
    ```typescript
    import { permanentRedirect } from "next/navigation";
    export default function ResearchHubRedirect() {
      permanentRedirect("/admin/hamilton");
    }
    ```

    Step 3 — Replace `src/app/admin/research/[agentId]/page.tsx`:
    Same pattern:
    ```typescript
    import { permanentRedirect } from "next/navigation";
    export default function ResearchAgentRedirect() {
      permanentRedirect("/admin/hamilton");
    }
    ```
    (Individual agent pages redirect to Hamilton chat where Hamilton handles all research routing internally per D-10.)

    Step 4 — Replace `src/app/admin/scout/page.tsx`:
    ```typescript
    import { permanentRedirect } from "next/navigation";
    export default function ScoutRedirect() {
      permanentRedirect("/admin/hamilton");
    }
    ```

    Note: Do NOT delete any files — only replace page content with redirects. The research API route (`/api/research/[agentId]`) and all `src/lib/research/` modules remain unchanged (Hamilton's tool layer reuses them). The research articles pages (`/admin/hamilton/research/articles`) remain functional — they are content management, not the Research Hub agent interface.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "admin-nav|research/page|scout/page" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `admin-nav.tsx` has Hamilton item pointing to `/admin/hamilton` with `exact` not set (or false) so sub-routes highlight it
    - Research Hub nav item removed from NAV_GROUPS
    - Scout nav item removed from NAV_GROUPS (if it existed)
    - `src/app/admin/research/page.tsx` calls `permanentRedirect("/admin/hamilton")`
    - `src/app/admin/research/[agentId]/page.tsx` calls `permanentRedirect("/admin/hamilton")`
    - `src/app/admin/scout/page.tsx` calls `permanentRedirect("/admin/hamilton")`
    - No TypeScript errors
    - Research articles page (`/admin/hamilton/research/articles`) is untouched
  </acceptance_criteria>
  <done>Nav simplified to single Hamilton entry; Research Hub and Scout pages permanently redirect to /admin/hamilton</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Hamilton Chat unified interface:
    - Plan 01: API route at /api/hamilton/chat + persistent memory (Supabase)
    - Plan 02: Chat tab UI at /admin/hamilton/chat with streaming, mini-reports, history sidebar
    - Plan 03: Two-tab layout, Reports sub-route, nav consolidation, redirects
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/admin/hamilton — confirm redirect to /admin/hamilton/chat
    2. On the Chat tab: type "What is the national overdraft median?" — confirm streaming response appears
    3. Type "Analyze overdraft fee trends in the Kansas banking market" — confirm a structured mini-report renders with amber accent card styling (not plain text)
    4. Check conversation sidebar — confirm the exchange was saved and appears in history
    5. Click "Reports" tab — confirm the report management UI (generation buttons, jobs table) loads correctly at /admin/hamilton/reports
    6. Visit http://localhost:3000/admin/research — confirm redirect to /admin/hamilton
    7. Visit http://localhost:3000/admin/scout — confirm redirect to /admin/hamilton
    8. Check admin nav — confirm single "Hamilton" entry; no "Research Hub" or "Scout" entries
    9. Confirm /admin/hamilton/research/articles still works (not broken by redirects)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → redirect pages | No user data processed — pure redirects |
| Tab navigation → URL routing | Client navigates to sub-routes; no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Information Disclosure | research/[agentId]/page.tsx redirect | accept | Page replaced with redirect; underlying API routes remain auth-gated; no data exposure |
| T-17-10 | Spoofing | Hamilton tab in admin-nav | accept | Nav is admin-only (admin layout gates auth); Hamilton tab inherits that gate |
</threat_model>

<verification>
Full TypeScript check:
```
npx tsc --noEmit 2>&1 | grep -c "error" 
```
Expected: 0 (or same as baseline before this phase).

Verify redirect content in research page:
```
grep -n "permanentRedirect" src/app/admin/research/page.tsx
grep -n "permanentRedirect" src/app/admin/scout/page.tsx
```

Verify Hamilton nav entry exists:
```
grep -n "hamilton" src/app/admin/admin-nav.tsx
```

Verify reports page has the original content (jobs table):
```
grep -n "ReportControls\|report_jobs\|Report Jobs" src/app/admin/hamilton/reports/page.tsx
```
</verification>

<success_criteria>
- Visiting `/admin/hamilton` redirects to `/admin/hamilton/chat` (308)
- Two-tab layout (Chat / Reports) renders correctly at `/admin/hamilton/*`
- Reports tab at `/admin/hamilton/reports` is fully functional (all existing report management works)
- Admin nav has single Hamilton entry; Research Hub and Scout are removed
- `/admin/research` and `/admin/scout` redirect permanently to `/admin/hamilton`
- `/admin/hamilton/research/articles` remains accessible (not broken)
- Zero TypeScript compilation errors introduced by this plan
- Human checkpoint: confirmed working by the user
</success_criteria>

<output>
After completion, create `.planning/phases/17-hamilton-chat-unified-research-interface/17-03-SUMMARY.md` with:
- Files modified and what changed in each
- Tab layout implementation approach (server vs client for active state)
- Confirmation of redirect pages created
- Nav changes made
- Human checkpoint outcome
- TypeScript compilation status
</output>
