---
phase: 32-scoped-report-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/pro/reports/page.tsx
  - src/components/pro/report-history-list.tsx
autonomous: false
requirements:
  - RPT-03
must_haves:
  truths:
    - "Pro subscriber sees all their previously generated reports listed chronologically at /pro/reports"
    - "Each report row shows type, date, status, and a download button for completed reports"
    - "Failed reports show a retry option that links back to /pro/reports/new"
    - "Only the current user's reports are visible (user-scoped)"
  artifacts:
    - path: "src/app/pro/reports/page.tsx"
      provides: "Report history server page with auth and data fetch"
      min_lines: 30
    - path: "src/components/pro/report-history-list.tsx"
      provides: "Client component for report history table with download/retry actions"
      exports: ["ReportHistoryList"]
  key_links:
    - from: "src/app/pro/reports/page.tsx"
      to: "report_jobs table"
      via: "SQL query with user_id filter"
      pattern: "user_id.*ORDER BY.*created_at"
    - from: "src/components/pro/report-history-list.tsx"
      to: "/api/reports/[id]/download"
      via: "download link"
      pattern: "api/reports.*download"
---

<objective>
Build the report history page at `/pro/reports` showing all reports generated by the current user, with type, date, status, and download/retry actions.

Purpose: Subscribers need to retrieve past reports without regenerating them. This completes the report lifecycle (generate -> track -> re-download).
Output: Report history page and list component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-scoped-report-generation/32-CONTEXT.md

@src/app/pro/reports/new/page.tsx
@src/app/api/reports/[id]/download/route.ts
@src/app/api/reports/[id]/status/route.ts
@src/lib/report-engine/types.ts
@src/lib/auth.ts
@src/lib/access.ts
@src/lib/crawler-db/connection.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/report-engine/types.ts:
```typescript
export type ReportType = 'national_index' | 'state_index' | 'peer_brief' | 'monthly_pulse';
export type ReportJobStatus = 'pending' | 'assembling' | 'rendering' | 'complete' | 'failed';
export interface ReportJob {
  id: string;
  report_type: ReportType;
  status: ReportJobStatus;
  params: Record<string, unknown> | null;
  artifact_key: string | null;
  error: string | null;
  created_at: string;
  completed_at: string | null;
  user_id: string | null;
}
```

From src/lib/auth.ts:
```typescript
export function getCurrentUser(): Promise<User | null>;
// User: { id, username, role, subscription_status, ... }
```

From src/lib/access.ts:
```typescript
export function canAccessPremium(user: User | null): boolean;
```

Download route: GET /api/reports/[id]/download returns 302 redirect to R2 presigned URL.

Warm palette colors:
- bg-page: #FDFBF7, bg-card: #FFFDF9, border: #E8DFD1
- accent: #C44B2E, text-primary: #1A1815, text-secondary: #7A7265
- text-muted: #A09788, font-heading: var(--font-newsreader)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Report history page and list component</name>
  <files>
    src/app/pro/reports/page.tsx
    src/components/pro/report-history-list.tsx
  </files>
  <read_first>
    src/app/pro/reports/new/page.tsx
    src/lib/report-engine/types.ts
    src/app/api/reports/[id]/download/route.ts
    src/lib/auth.ts
    src/lib/access.ts
    src/lib/crawler-db/connection.ts
  </read_first>
  <action>
Create two files:

**1. `src/app/pro/reports/page.tsx`** -- Server component (per D-07, D-09).

```
export const dynamic = 'force-dynamic';
```

Auth: `getCurrentUser()` + `canAccessPremium()` -- redirect if not authorized.

Query report_jobs for current user using `getSql()`:
```sql
SELECT id, report_type, status, params, error, created_at, completed_at
FROM report_jobs
WHERE user_id = ${user.id}
ORDER BY created_at DESC
LIMIT 50
```

Render page with warm palette:
- Header section (same pattern as /pro/reports/new):
  - Tag: "Reports" in #C44B2E uppercase with leading rule div
  - Title: "Report History" in newsreader font, 3xl bold
  - Subtitle: "Your previously generated reports. Completed reports can be re-downloaded anytime."
  - Link to "/pro/reports/new" as primary CTA button: "Generate New Report" with #C44B2E bg
- Content: `<ReportHistoryList reports={rows} />` inside max-w-4xl container

Metadata: `title: 'Report History | Bank Fee Index'`

**2. `src/components/pro/report-history-list.tsx`** -- Client component ("use client") (per D-07, D-08).

Props: `reports: Array<{ id: string; report_type: string; status: string; error: string | null; created_at: string; completed_at: string | null }>`.

Display as a clean table/list with warm palette styling:
- Each row: rounded border card or table row
- Columns:
  - **Type**: Display name mapping: `peer_brief` -> "Peer Brief", `state_index` -> "District Outlook", `national_index` -> "National Index", `monthly_pulse` -> "Monthly Pulse"
  - **Date**: `new Date(created_at).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: 'numeric', minute: '2-digit' })`
  - **Status**: Badge with status-appropriate color:
    - `complete`: emerald-50/emerald-600 "Complete"
    - `failed`: red-50/red-600 "Failed"
    - `pending`/`assembling`/`rendering`: amber-50/amber-600 "In Progress"
  - **Action**:
    - Complete: Download link `<a href="/api/reports/${report.id}/download">` with download icon. Style as #C44B2E text link.
    - Failed: "Retry" link to `/pro/reports/new` styled as subtle text link.
    - In progress: "Generating..." muted text.

Empty state (no reports): Centered message "No reports yet" with a link to "/pro/reports/new" to generate their first.

Table header uses `text-[10px] font-bold uppercase tracking-wider text-[#A09788]` pattern.
Row hover: `hover:bg-[#FAF7F2] transition-colors`.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/pro/reports/page.tsx src/components/pro/report-history-list.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "force-dynamic" src/app/pro/reports/page.tsx
    - grep -q "getCurrentUser" src/app/pro/reports/page.tsx
    - grep -q "canAccessPremium" src/app/pro/reports/page.tsx
    - grep -q "report_jobs" src/app/pro/reports/page.tsx
    - grep -q "user_id" src/app/pro/reports/page.tsx
    - grep -q "ORDER BY" src/app/pro/reports/page.tsx
    - grep -q "ReportHistoryList" src/app/pro/reports/page.tsx
    - grep -q "use client" src/components/pro/report-history-list.tsx
    - grep -q "api/reports.*download" src/components/pro/report-history-list.tsx
    - grep -q "complete" src/components/pro/report-history-list.tsx
    - grep -q "failed" src/components/pro/report-history-list.tsx
    - grep -q "/pro/reports/new" src/components/pro/report-history-list.tsx
  </acceptance_criteria>
  <done>
    - /pro/reports page shows all reports for the current user, ordered by newest first
    - Completed reports have a working download link to /api/reports/[id]/download
    - Failed reports show a retry link to /pro/reports/new
    - In-progress reports show "Generating..." status
    - Empty state guides user to generate their first report
    - Auth gate prevents non-premium users from accessing the page
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of report generation flow</name>
  <files>src/app/pro/reports/new/page.tsx, src/app/pro/reports/page.tsx</files>
  <action>
Human verifies the complete report generation UI built across Plans 01 and 02:
- Report type selector (3 cards) at /pro/reports/new
- Scope forms per report type with peer filter controls
- Generation trigger with progress indicator
- Daily limit enforcement UI
- Report history page at /pro/reports with download/retry actions
- Warm palette consistency across all new components

Verification steps:
1. Navigate to `/pro/reports/new` as a premium user
2. Verify 3 report type cards are visible: Peer Brief, Competitive Snapshot, District Outlook
3. Click Peer Brief -- verify charter/tier/district filter form appears
4. Click Back -- verify return to type selection
5. Click District Outlook -- verify district-only selector appears
6. Generate a report (if pipeline is connected) or verify the generate button calls the API
7. Navigate to `/pro/reports` -- verify report history page loads with any existing reports
8. Verify completed reports have download links, failed have retry links
9. Verify warm palette is consistent (cream backgrounds, terracotta accent)
  </action>
  <verify>Human visual inspection of report generation flow and history page</verify>
  <done>User confirms report type selection, scope forms, generation progress, and history page are visually correct and functionally working</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> DB | User ID used in WHERE clause for report_jobs query |
| client -> /api/reports/[id]/download | Job ID from UI used in download route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Information Disclosure | /pro/reports page | mitigate | SQL query filters by user_id from authenticated session; user cannot see others' reports |
| T-32-06 | Spoofing | download link | mitigate | Existing download route verifies ownership (isOwner check) before generating presigned URL |
| T-32-07 | Tampering | report history | accept | Report list is read-only; no mutation endpoints exposed from this page |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `/pro/reports` loads and shows user's report history
3. Completed reports download via presigned URL redirect
4. Failed reports link to /pro/reports/new for retry
5. Non-premium users redirected away from both pages
</verification>

<success_criteria>
- Report history page shows all user reports with type, date, status, and actions
- Download works for completed reports via existing presigned URL flow
- Failed reports offer retry path
- User-scoped queries prevent cross-user data leakage
- Visual consistency with warm palette design system
</success_criteria>

<output>
After completion, create `.planning/phases/32-scoped-report-generation/32-02-SUMMARY.md`
</output>
