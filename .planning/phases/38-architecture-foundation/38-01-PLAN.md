---
phase: 38-architecture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "Styles applied inside .hamilton-shell do not bleed into admin portal or consumer pages"
    - "Hamilton editorial tokens (parchment surface, Newsreader serif, burnished terracotta accent) are scoped exclusively to .hamilton-shell elements"
    - "Dark mode for Hamilton uses warm dark parchment tones following the same .dark .hamilton-shell pattern as .dark .admin-content"
  artifacts:
    - path: "src/app/globals.css"
      provides: ".hamilton-shell CSS isolation block with design tokens and dark mode overrides"
      contains: ".hamilton-shell"
  key_links:
    - from: "src/app/globals.css"
      to: "Phase 40 hamilton layout"
      via: ".hamilton-shell class applied to layout wrapper"
      pattern: "\\.hamilton-shell"
---

<objective>
Add the `.hamilton-shell` CSS isolation boundary to globals.css with all editorial design tokens (warm parchment palette, Newsreader serif, burnished terracotta accent) and dark mode overrides.

Purpose: Every downstream Hamilton screen (Phases 40-46) wraps in `.hamilton-shell` to get the editorial aesthetic without contaminating admin or consumer styles.
Output: Extended globals.css with `.hamilton-shell` block, dark mode overrides, and Hamilton-specific utility classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-architecture-foundation/38-CONTEXT.md
@.planning/phases/38-architecture-foundation/38-RESEARCH.md

@src/app/globals.css
@Hamilton-Design/hamilton_sovereign/DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .hamilton-shell CSS isolation block with design tokens and dark mode</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current state — append after existing content, never modify existing rules)
    - Hamilton-Design/hamilton_sovereign/DESIGN.md (full design system spec — extract exact color values, typography, elevation tokens)
    - Hamilton-Design/1-executive_home_briefing_final_polish/screen.png (visual target — verify parchment warmth)
  </read_first>
  <action>
Append a clearly delimited `.hamilton-shell` block to the END of globals.css. Do NOT modify any existing CSS rules above. Per D-01, D-02, D-03, D-04.

**Section 1: Hamilton design tokens (light mode)**
Add inside `.hamilton-shell { ... }`:

```css
/* Surface tokens */
--hamilton-surface: #fbf9f4;
--hamilton-surface-elevated: #f5f1e8;
--hamilton-surface-sunken: #f0ebe0;
--hamilton-surface-overlay: rgba(251, 249, 244, 0.95);

/* Text tokens */
--hamilton-text-primary: #1c1917;
--hamilton-text-secondary: #78716c;
--hamilton-text-tertiary: #a8a29e;
--hamilton-text-accent: oklch(0.55 0.18 35);

/* Accent (burnished terracotta) */
--hamilton-accent: oklch(0.55 0.18 35);
--hamilton-accent-hover: oklch(0.50 0.20 35);
--hamilton-accent-subtle: oklch(0.55 0.18 35 / 0.08);

/* Burnished gradient for CTAs */
--hamilton-gradient-cta: linear-gradient(135deg, oklch(0.55 0.18 35), oklch(0.48 0.16 30));

/* Typography */
--hamilton-font-serif: var(--font-newsreader), Georgia, serif;
--hamilton-font-sans: var(--font-geist-sans), ui-sans-serif, system-ui, sans-serif;

/* Elevation (tonal layering, no borders per D-01) */
--hamilton-shadow-card: 0 1px 3px rgba(28, 25, 23, 0.04), 0 1px 2px rgba(28, 25, 23, 0.02);
--hamilton-shadow-elevated: 0 4px 12px rgba(28, 25, 23, 0.06), 0 1px 3px rgba(28, 25, 23, 0.03);
--hamilton-shadow-overlay: 0 8px 24px rgba(28, 25, 23, 0.08), 0 2px 6px rgba(28, 25, 23, 0.04);

/* Border (subtle warm, used sparingly) */
--hamilton-border: rgba(28, 25, 23, 0.06);
--hamilton-border-hover: rgba(28, 25, 23, 0.12);
```

Apply base styles on `.hamilton-shell` itself:
```css
background: var(--hamilton-surface);
color: var(--hamilton-text-primary);
font-family: var(--hamilton-font-sans);
```

**Section 2: Dark mode overrides**
Add `.dark .hamilton-shell { ... }` block with warm dark parchment values:

```css
--hamilton-surface: #1c1917;
--hamilton-surface-elevated: #292524;
--hamilton-surface-sunken: #171412;
--hamilton-surface-overlay: rgba(28, 25, 23, 0.95);
--hamilton-text-primary: oklch(0.93 0.01 60);
--hamilton-text-secondary: oklch(0.65 0.01 60);
--hamilton-text-tertiary: oklch(0.50 0.01 60);
--hamilton-text-accent: oklch(0.70 0.15 35);
--hamilton-accent: oklch(0.70 0.15 35);
--hamilton-accent-hover: oklch(0.75 0.17 35);
--hamilton-accent-subtle: oklch(0.70 0.15 35 / 0.12);
--hamilton-gradient-cta: linear-gradient(135deg, oklch(0.65 0.15 35), oklch(0.55 0.13 30));
--hamilton-shadow-card: 0 1px 3px rgba(0, 0, 0, 0.3), 0 1px 2px rgba(0, 0, 0, 0.2);
--hamilton-shadow-elevated: 0 4px 12px rgba(0, 0, 0, 0.4), 0 1px 3px rgba(0, 0, 0, 0.3);
--hamilton-shadow-overlay: 0 8px 24px rgba(0, 0, 0, 0.5), 0 2px 6px rgba(0, 0, 0, 0.3);
--hamilton-border: rgba(255, 255, 255, 0.06);
--hamilton-border-hover: rgba(255, 255, 255, 0.12);
```

**Section 3: Hamilton utility classes**
Add scoped utility classes for common editorial patterns:

```css
/* Hamilton card — tonal elevation, no hard borders */
.hamilton-card {
  background: var(--hamilton-surface-elevated);
  border-radius: 0.75rem;
  box-shadow: var(--hamilton-shadow-card);
  transition: box-shadow 0.2s ease;
}
.hamilton-card:hover {
  box-shadow: var(--hamilton-shadow-elevated);
}

/* Hamilton serif headings */
.hamilton-shell h1,
.hamilton-shell h2,
.hamilton-shell h3 {
  font-family: var(--hamilton-font-serif);
}

/* Hamilton staggered reveal (same pattern as admin-content) */
.hamilton-shell > * {
  animation: admin-fade-up 0.4s cubic-bezier(0.16, 1, 0.3, 1) both;
}
.hamilton-shell > *:nth-child(1) { animation-delay: 0ms; }
.hamilton-shell > *:nth-child(2) { animation-delay: 50ms; }
.hamilton-shell > *:nth-child(3) { animation-delay: 60ms; }
.hamilton-shell > *:nth-child(4) { animation-delay: 70ms; }
.hamilton-shell > *:nth-child(5) { animation-delay: 80ms; }

/* Reduce motion */
@media (prefers-reduced-motion: reduce) {
  .hamilton-shell > * {
    animation: none !important;
  }
}
```

**IMPORTANT per D-03:** Do NOT put any `--hamilton-*` variables in the `@theme inline` block. They must be scoped inside `.hamilton-shell` only.

**IMPORTANT per D-05:** The string "Sovereign Intelligence" must NOT appear anywhere in the CSS comments or code.
  </action>
  <verify>
    <automated>grep -c "\.hamilton-shell" src/app/globals.css | xargs test 3 -le && echo "PASS: hamilton-shell block exists" && grep -c "\.dark .hamilton-shell" src/app/globals.css | xargs test 1 -le && echo "PASS: dark mode block exists" && ! grep -q "Sovereign" src/app/globals.css && echo "PASS: no Sovereign branding" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `.hamilton-shell {` with `--hamilton-surface: #fbf9f4`
    - globals.css contains `.dark .hamilton-shell {` with `--hamilton-surface: #1c1917`
    - globals.css contains `--hamilton-font-serif: var(--font-newsreader)`
    - globals.css contains `--hamilton-accent: oklch(0.55 0.18 35)`
    - globals.css contains `.hamilton-card {` utility class
    - globals.css contains `.hamilton-shell h1` serif heading rule
    - globals.css does NOT contain the string "Sovereign"
    - No `--hamilton-*` variables appear inside `@theme inline` block
    - `npx tsc --noEmit` passes (no compile errors introduced)
    - All existing CSS rules above the new block are unchanged (no deletions, no modifications)
  </acceptance_criteria>
  <done>Hamilton CSS isolation boundary is complete: .hamilton-shell scoping with all design tokens (surface, text, accent, typography, elevation), dark mode overrides, and editorial utility classes. No style bleed into admin or consumer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CSS isolation smoke tests</name>
  <files>src/lib/hamilton/css-tokens.test.ts</files>
  <read_first>
    - src/app/globals.css (verify the .hamilton-shell block from Task 1 is present)
    - src/lib/hamilton/types.test.ts (existing test pattern to follow)
  </read_first>
  <behavior>
    - Test 1: globals.css contains .hamilton-shell block with --hamilton-surface token
    - Test 2: globals.css contains .dark .hamilton-shell dark mode block
    - Test 3: globals.css does NOT contain --hamilton-* inside @theme inline block (isolation check)
    - Test 4: globals.css does NOT contain "Sovereign" string
  </behavior>
  <action>
Create `src/lib/hamilton/css-tokens.test.ts` that reads globals.css as a string and verifies CSS isolation invariants. This is a file-content test (not a browser test) that catches regressions if someone moves Hamilton tokens to @theme.

Use `fs.readFileSync` to load `src/app/globals.css` and run string assertions:

1. Assert `.hamilton-shell {` exists in file
2. Assert `.dark .hamilton-shell {` exists in file
3. Assert `--hamilton-surface` appears inside a `.hamilton-shell` block (not at root)
4. Assert `--hamilton-` does NOT appear between `@theme inline {` and its closing `}`
5. Assert `Sovereign` does NOT appear anywhere in the file

Import `describe, it, expect` from vitest. Use `path.resolve(__dirname, '../../../app/globals.css')` for the file path (respects `@/*` alias structure).
  </action>
  <verify>
    <automated>npx vitest run src/lib/hamilton/css-tokens.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/hamilton/css-tokens.test.ts` exists
    - Contains `describe("hamilton CSS isolation"` or similar
    - Contains assertion checking `.hamilton-shell {` presence
    - Contains assertion checking `@theme inline` does NOT contain `--hamilton-`
    - `npx vitest run src/lib/hamilton/css-tokens.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>CSS isolation invariants are automatically tested — any future regression (tokens leaking to @theme, missing dark mode, branding violations) is caught by vitest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS cascade | Hamilton tokens must not leak to :root or @theme (global scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information Disclosure | CSS tokens on :root | mitigate | All --hamilton-* tokens scoped inside .hamilton-shell selector only; css-tokens.test.ts verifies no leak to @theme |
| T-38-02 | Tampering | Existing admin styles modified | accept | Low risk — append-only change to globals.css; visual regression caught by human QA in Phase 40 |
</threat_model>

<verification>
1. `grep -c "\.hamilton-shell" src/app/globals.css` returns 3+ (main block, dark block, utility selectors)
2. `grep -c "\.dark .hamilton-shell" src/app/globals.css` returns 1+
3. `npx vitest run src/lib/hamilton/css-tokens.test.ts` passes
4. `npx tsc --noEmit` passes with zero errors
</verification>

<success_criteria>
- .hamilton-shell CSS block exists with all editorial design tokens
- Dark mode .dark .hamilton-shell block exists with warm dark parchment values
- No --hamilton-* variables appear in @theme inline block
- No "Sovereign Intelligence" branding in CSS
- Automated CSS isolation tests pass
- TypeScript compilation unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/38-architecture-foundation/38-01-SUMMARY.md`
</output>
