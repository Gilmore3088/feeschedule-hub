---
phase: 48-pro-navigation-full-canvas-width
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/pro/(hamilton)/monitor/page.tsx
  - src/app/pro/(hamilton)/settings/page.tsx
  - src/app/pro/(hamilton)/hamilton/page.tsx
autonomous: false
requirements:
  - NAV-02
  - MON-04
must_haves:
  truths:
    - "Monitor page has no maxWidth or max-w on its header or content grid wrappers"
    - "Settings page has no max-w-5xl mx-auto on its outermost content wrapper"
    - "Home (hamilton) page has no maxWidth constraint on its outermost content wrapper"
    - "All three pages render full-width without horizontal scroll"
  artifacts:
    - path: "src/app/pro/(hamilton)/monitor/page.tsx"
      provides: "Full-width Monitor layout"
    - path: "src/app/pro/(hamilton)/settings/page.tsx"
      provides: "Full-width Settings layout"
    - path: "src/app/pro/(hamilton)/hamilton/page.tsx"
      provides: "Full-width Home layout"
  key_links:
    - from: "HamiltonShell main (flex-1 min-w-0 p-6)"
      to: "Individual page content"
      via: "No intermediate maxWidth wrappers"
      pattern: "flex-1 min-w-0"
---

<objective>
Remove all outer-level maxWidth and max-w-* CSS constraints from Hamilton screen pages (Monitor, Settings, Home) so content fills the full browser canvas width edge-to-edge within the HamiltonShell main area.

Purpose: Per D-05, D-06, D-07 — every Hamilton screen must use the full browser canvas width with no wasted margins, without introducing horizontal scroll.
Output: Three updated page files with full-width layouts.
</objective>

<execution_context>
@/Users/jgmbp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jgmbp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-pro-navigation-full-canvas-width/48-CONTEXT.md
@.planning/phases/48-pro-navigation-full-canvas-width/48-RESEARCH.md

<interfaces>
<!-- HamiltonShell provides the outer frame. Pages render inside its main tag. -->
From src/components/hamilton/layout/HamiltonShell.tsx:
- main tag: className="flex-1 min-w-0 p-6" — this p-6 provides breathing room
- Pages should NOT add their own maxWidth/margin:auto wrappers on top-level elements

Constraints to KEEP (inner content, not page-level):
- monitor/page.tsx line 60: maxWidth: "42rem" on subtitle paragraph (text readability)
- settings/page.tsx line 78: max-w-xl on subtitle paragraph (text readability)
- AnalyzeWorkspace.tsx lines 238/289: max-w-5xl/max-w-4xl on chat content (readability)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove outer maxWidth constraints from Monitor, Settings, and Home pages</name>
  <files>
    src/app/pro/(hamilton)/monitor/page.tsx
    src/app/pro/(hamilton)/settings/page.tsx
    src/app/pro/(hamilton)/hamilton/page.tsx
  </files>
  <read_first>
    src/app/pro/(hamilton)/monitor/page.tsx
    src/app/pro/(hamilton)/settings/page.tsx
    src/app/pro/(hamilton)/hamilton/page.tsx
    src/components/hamilton/layout/HamiltonShell.tsx
  </read_first>
  <action>
Per D-05 (full canvas width), D-06 (Settings max-w removal), D-07 (no horizontal scroll), and MON-04 (Monitor full width):

**Monitor page (src/app/pro/(hamilton)/monitor/page.tsx):**

1. Line 39 — header has `maxWidth: "72rem", margin: "0 auto 3rem"`. Change to:
   ```tsx
   <header style={{ marginBottom: "3rem" }}>
   ```
   Remove `maxWidth: "72rem"` and `margin: "0 auto 3rem"`, keep only `marginBottom: "3rem"`.

2. Line 60 — subtitle paragraph has `maxWidth: "42rem"`. KEEP THIS — it controls text line length for readability, not page layout.

3. Lines 70-77 — content grid has `maxWidth: "72rem", margin: "0 auto"`. Change to:
   ```tsx
   <div style={{
     display: "grid",
     gridTemplateColumns: "7fr 5fr",
     gap: "3rem",
   }}>
   ```
   Remove `maxWidth: "72rem"` and `margin: "0 auto"`.

**Settings page (src/app/pro/(hamilton)/settings/page.tsx):**

4. Line 63 — outermost content wrapper has `className="max-w-5xl mx-auto pb-16"`. Change to:
   ```tsx
   <div className="pb-16">
   ```
   Remove `max-w-5xl mx-auto` per D-06. Keep `pb-16`.

5. Line 78 — subtitle has `className="text-sm max-w-xl"`. KEEP THIS — text readability constraint, not page layout.

**Home page (src/app/pro/(hamilton)/hamilton/page.tsx):**

6. Line 81 — outermost content wrapper has `style={{ padding: "2rem 3rem", maxWidth: "100rem", margin: "0 auto" }}`. Change to:
   ```tsx
   <div style={{ padding: "2rem 3rem" }}>
   ```
   Remove `maxWidth: "100rem"` and `margin: "0 auto"`. Keep `padding: "2rem 3rem"` for breathing room.

**What NOT to touch:**
- HamiltonShell.tsx `p-6` on main tag — this is correct outer padding
- AnalyzeWorkspace.tsx `max-w-5xl` and `max-w-4xl` — these are inner chat content constraints
- Any `max-w-xl` on text paragraphs — readability constraints, not layout constraints
  </action>
  <verify>
    <automated>grep -n "maxWidth\|max-w-5xl\|max-w-7xl" src/app/pro/\(hamilton\)/monitor/page.tsx src/app/pro/\(hamilton\)/settings/page.tsx src/app/pro/\(hamilton\)/hamilton/page.tsx 2>/dev/null | grep -v "42rem" | grep -v "max-w-xl"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "72rem" src/app/pro/\(hamilton\)/monitor/page.tsx` returns NO matches (both 72rem constraints removed)
    - `grep "max-w-5xl" src/app/pro/\(hamilton\)/settings/page.tsx` returns NO matches
    - `grep "100rem" src/app/pro/\(hamilton\)/hamilton/page.tsx` returns NO matches
    - `grep "42rem" src/app/pro/\(hamilton\)/monitor/page.tsx` returns 1 match (subtitle kept)
    - `grep "max-w-xl" src/app/pro/\(hamilton\)/settings/page.tsx` returns 1 match (subtitle kept)
    - `npx tsc --noEmit 2>&1 | head -20` passes with no errors
  </acceptance_criteria>
  <done>All three Hamilton pages render at full canvas width. Monitor header and grid, Settings outer wrapper, and Home outer wrapper no longer have width constraints. Text readability constraints on subtitle paragraphs are preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of full-width Hamilton screens</name>
  <files>none (verification only)</files>
  <action>
Human visually verifies that all Hamilton screens render at full canvas width without horizontal scroll, and that old Pro route redirects work correctly. This checkpoint covers both Plan 01 (route cleanup) and Plan 02 (full-width layout) changes.
  </action>
  <what-built>Full canvas width layout applied to Monitor, Settings, and Home pages. Old Pro route directories deleted with permanent redirects configured.</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Log in as a premium/admin user
    3. Visit `/pro/monitor` — confirm content fills the full browser width, no centered narrow column. Resize browser between 1280px and 1920px, confirm no horizontal scroll.
    4. Visit `/pro/hamilton` (Home) — confirm content fills full width, no centered narrow column.
    5. Visit `/pro/settings` — confirm settings page fills full width, no centered narrow column.
    6. Visit `/pro/analyze` — confirm the workspace renders correctly (its inner content constraints should be intact).
    7. Test a redirect: visit `/pro/categories` — confirm it redirects to `/pro/monitor` (not a 404).
    8. Test another redirect: visit `/pro/research` — confirm it redirects to `/pro/analyze`.
  </how-to-verify>
  <verify>Human visual inspection at multiple viewport widths</verify>
  <done>All Hamilton screens confirmed full-width. No horizontal scroll. Redirects work. User approved.</done>
  <resume-signal>Type "approved" or describe layout issues (e.g., "Monitor grid too wide at 1920px", "Settings form needs max-width on inputs")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan modifies only CSS layout properties on authenticated pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03 | Information Disclosure | Wider layout | accept | No new data exposed; same data rendered at wider viewport — auth gating unchanged |
</threat_model>

<verification>
1. `grep -c "72rem" src/app/pro/\(hamilton\)/monitor/page.tsx` returns 0
2. `grep -c "max-w-5xl" src/app/pro/\(hamilton\)/settings/page.tsx` returns 0
3. `grep -c "100rem" src/app/pro/\(hamilton\)/hamilton/page.tsx` returns 0
4. `npx vitest run` passes
5. Visual check at 1280px and 1920px shows no horizontal scroll on any Hamilton screen
</verification>

<success_criteria>
- Monitor, Settings, and Home pages use full canvas width
- No horizontal scroll at any standard viewport width
- Text readability constraints on subtitle paragraphs are preserved
- Human verifies visual layout is correct across screen sizes
</success_criteria>

<output>
After completion, create `.planning/phases/48-pro-navigation-full-canvas-width/48-02-SUMMARY.md`
</output>
