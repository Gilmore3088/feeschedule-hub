---
phase: 48-pro-navigation-full-canvas-width
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/app/pro/categories/page.tsx
  - src/app/pro/peers/page.tsx
  - src/app/pro/peers/actions.ts
  - src/app/pro/peers/saved-groups.tsx
  - src/app/pro/market/page.tsx
  - src/app/pro/districts/page.tsx
  - src/app/pro/data/page.tsx
  - src/app/pro/news/page.tsx
  - src/app/pro/news/actions.ts
  - src/app/pro/news/news-feed.tsx
  - src/app/pro/research/page.tsx
  - src/app/pro/research/analyst-hub.tsx
  - src/app/pro/reports-legacy/page.tsx
  - src/app/pro/brief/route.ts
  - src/app/pro/brief/preview/route.ts
  - src/app/pro/dashboard.tsx
  - src/components/pro-nav.tsx
  - src/app/pro/loading.tsx
autonomous: true
requirements:
  - NAV-01
must_haves:
  truths:
    - "Visiting /pro/categories in a browser returns a 308 redirect to /pro/monitor"
    - "Visiting /pro/peers returns a 308 redirect to /pro/monitor"
    - "Visiting /pro/market returns a 308 redirect to /pro/analyze"
    - "Visiting /pro/districts returns a 308 redirect to /pro/analyze"
    - "Visiting /pro/data returns a 308 redirect to /pro/analyze"
    - "Visiting /pro/news returns a 308 redirect to /pro/monitor"
    - "Visiting /pro/research returns a 308 redirect to /pro/analyze"
    - "Visiting /pro/reports-legacy returns a 308 redirect to /pro/reports"
    - "No old Pro tab route directories exist under src/app/pro/"
    - "The pro-nav.tsx component file does not exist"
  artifacts:
    - path: "next.config.ts"
      provides: "Permanent redirects for all deleted Pro routes"
      contains: "async redirects"
  key_links:
    - from: "next.config.ts redirects"
      to: "/pro/monitor, /pro/analyze, /pro/reports"
      via: "permanent: true redirect entries"
      pattern: "permanent.*true"
---

<objective>
Delete all legacy Pro tab routes (categories, peers, market, districts, data, news, research, reports-legacy, brief) and add permanent redirects in next.config.ts so bookmarked URLs resolve to the correct Hamilton screen. Also delete dead navigation components (pro-nav.tsx) and update the Pro loading skeleton for full-width.

Purpose: The Hamilton 5-screen system IS the entire Pro experience (per D-02). Old route files are dead weight and confuse the codebase.
Output: Clean src/app/pro/ directory with only layout.tsx, page.tsx, loading.tsx, and the (hamilton)/ route group.
</objective>

<execution_context>
@/Users/jgmbp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jgmbp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-pro-navigation-full-canvas-width/48-CONTEXT.md
@.planning/phases/48-pro-navigation-full-canvas-width/48-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old Pro route directories and dead nav components</name>
  <files>
    src/app/pro/categories/ (DELETE entire directory)
    src/app/pro/peers/ (DELETE entire directory)
    src/app/pro/market/ (DELETE entire directory)
    src/app/pro/districts/ (DELETE entire directory)
    src/app/pro/data/ (DELETE entire directory)
    src/app/pro/news/ (DELETE entire directory)
    src/app/pro/research/ (DELETE entire directory)
    src/app/pro/reports-legacy/ (DELETE entire directory)
    src/app/pro/brief/ (DELETE entire directory)
    src/app/pro/dashboard.tsx (DELETE file)
    src/components/pro-nav.tsx (DELETE file)
  </files>
  <read_first>
    src/app/pro/peers/actions.ts
    src/components/pro-nav.tsx
    src/app/pro/dashboard.tsx
    src/app/pro/brief/route.ts
  </read_first>
  <action>
Per D-01 (old Pro tabs ELIMINATED) and D-02 (Hamilton 5-screen system IS the entire Pro experience):

1. Run a final safety grep to confirm no active Hamilton code imports from any of these files:
   - `grep -r "pro/peers/actions" src/app/pro/\(hamilton\)/ src/components/hamilton/` (expect 0 results)
   - `grep -r "pro/brief" src/app/pro/\(hamilton\)/ src/components/hamilton/` (expect 0 results)
   - `grep -r "pro/dashboard" src/app/pro/\(hamilton\)/ src/components/hamilton/` (expect 0 results)
   - `grep -r "pro-nav" src/app/` (expect 0 results — pro-nav.tsx is not imported anywhere)

2. Delete entire directories:
   - `rm -rf src/app/pro/categories/`
   - `rm -rf src/app/pro/peers/`
   - `rm -rf src/app/pro/market/`
   - `rm -rf src/app/pro/districts/`
   - `rm -rf src/app/pro/data/`
   - `rm -rf src/app/pro/news/`
   - `rm -rf src/app/pro/research/`
   - `rm -rf src/app/pro/reports-legacy/`
   - `rm -rf src/app/pro/brief/`

3. Delete standalone files:
   - `rm src/app/pro/dashboard.tsx`
   - `rm src/components/pro-nav.tsx`

4. Also check for `src/components/pro-mobile-nav.tsx` — if it exists and has zero imports in src/app/, delete it too.

After deletion, `ls src/app/pro/` should show ONLY: `(hamilton)/`, `layout.tsx`, `page.tsx`, `loading.tsx`
  </action>
  <verify>
    <automated>ls src/app/pro/ | sort</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/app/pro/` shows exactly: `(hamilton)  layout.tsx  loading.tsx  page.tsx` (no categories, peers, market, districts, data, news, research, reports-legacy, brief, or dashboard.tsx)
    - `ls src/components/pro-nav.tsx 2>/dev/null` returns nothing (file deleted)
    - `npx tsc --noEmit 2>&1 | head -20` shows no import errors from deleted files
  </acceptance_criteria>
  <done>All legacy Pro tab route directories and dead navigation components are deleted. Only Hamilton route group and essential Pro files remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add permanent redirects in next.config.ts and update loading skeleton</name>
  <files>next.config.ts, src/app/pro/loading.tsx</files>
  <read_first>next.config.ts, src/app/pro/loading.tsx</read_first>
  <action>
Per D-04 (old Pro routes redirect to Hamilton screens to avoid 404s):

1. In `next.config.ts`, add an `async redirects()` function inside the `nextConfig` object, BEFORE the existing `async headers()`. Add these permanent redirect entries:

```typescript
async redirects() {
  return [
    { source: '/pro/categories', destination: '/pro/monitor', permanent: true },
    { source: '/pro/peers', destination: '/pro/monitor', permanent: true },
    { source: '/pro/market', destination: '/pro/analyze', permanent: true },
    { source: '/pro/districts', destination: '/pro/analyze', permanent: true },
    { source: '/pro/data', destination: '/pro/analyze', permanent: true },
    { source: '/pro/news', destination: '/pro/monitor', permanent: true },
    { source: '/pro/research', destination: '/pro/analyze', permanent: true },
    { source: '/pro/reports-legacy', destination: '/pro/reports', permanent: true },
    { source: '/pro/brief', destination: '/pro/monitor', permanent: true },
    { source: '/pro/brief/:path*', destination: '/pro/monitor', permanent: true },
  ];
},
```

Redirect mapping rationale:
- categories, peers, news -> /pro/monitor (signal/watchlist context)
- market, districts, data, research -> /pro/analyze (data analysis context)
- reports-legacy -> /pro/reports (direct replacement)
- brief and brief subpaths -> /pro/monitor (briefing context)

2. Update `src/app/pro/loading.tsx` to remove the `max-w-7xl` constraint per D-05/D-07:

Change line 3 from:
```tsx
<div className="mx-auto max-w-7xl px-6 py-16">
```
to:
```tsx
<div className="px-6 py-16">
```

This loading skeleton applies to the pro route group before Hamilton shell mounts. Full width is correct per D-05.
  </action>
  <verify>
    <automated>grep -c "async redirects" next.config.ts && grep -c "permanent: true" next.config.ts && grep "max-w" src/app/pro/loading.tsx; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "async redirects" next.config.ts` returns a match
    - `grep -c "permanent: true" next.config.ts` returns 10 (one per redirect)
    - `grep "/pro/categories" next.config.ts` returns the redirect entry
    - `grep "max-w" src/app/pro/loading.tsx` returns NO matches (constraint removed)
    - `npx tsc --noEmit 2>&1 | head -20` shows no TypeScript errors
  </acceptance_criteria>
  <done>All 10 permanent redirects are in next.config.ts. Pro loading skeleton uses full width. Old bookmarked URLs resolve to appropriate Hamilton screens instead of 404.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> next.config.ts redirects | Untrusted URLs from bookmarks/crawlers hit redirect rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Spoofing | Redirect destinations | accept | All destinations are internal Hamilton routes already behind auth; no open redirect risk since destinations are hardcoded |
| T-48-02 | Information Disclosure | Deleted route files | accept | Files are removed from the codebase; no sensitive data was in these route files (they used DB queries, not secrets) |
</threat_model>

<verification>
1. `ls src/app/pro/` shows only `(hamilton)/ layout.tsx loading.tsx page.tsx`
2. `grep -c "permanent: true" next.config.ts` returns 10
3. `npx tsc --noEmit` passes with no errors related to deleted files
4. `npx vitest run` passes (existing tests unaffected)
</verification>

<success_criteria>
- All 9 old Pro route directories and 2 dead component files are deleted
- 10 permanent redirects exist in next.config.ts mapping old routes to Hamilton screens
- Pro loading skeleton has no max-w constraint
- TypeScript compilation succeeds
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/48-pro-navigation-full-canvas-width/48-01-SUMMARY.md`
</output>
