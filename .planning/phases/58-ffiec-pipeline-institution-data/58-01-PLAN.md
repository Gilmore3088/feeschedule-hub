---
phase: 58-ffiec-pipeline-institution-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fee_crawler/commands/ingest_call_reports.py
  - fee_crawler/commands/ingest_ncua.py
  - fee_crawler/modal_app.py
  - fee_crawler/__main__.py
autonomous: true
requirements:
  - COV-03
user_setup: []

must_haves:
  truths:
    - "Running `python -m fee_crawler ingest-call-reports` connects to Postgres and upserts FFIEC CDR rows into institution_financials"
    - "Running `python -m fee_crawler ingest-ncua` connects to Postgres and upserts NCUA 5300 rows into institution_financials"
    - "Running `python -m fee_crawler ingest-call-reports --backfill --from-year 2010` downloads and inserts historical quarterly data year by year"
    - "Running `python -m fee_crawler ingest-ncua --backfill --from-year 2010` downloads and inserts NCUA historical quarterly data"
    - "The Modal ingest_data function runs FFIEC and NCUA ingestion quarterly (Feb/May/Aug/Nov 15th) in addition to weekly"
    - "Download failures retry 3 times with exponential backoff before logging error and continuing"
  artifacts:
    - path: "fee_crawler/commands/ingest_call_reports.py"
      provides: "FFIEC CDR ingestion with psycopg2 Postgres connection"
      contains: "psycopg2.connect"
    - path: "fee_crawler/commands/ingest_ncua.py"
      provides: "NCUA 5300 ingestion with psycopg2 Postgres connection"
      contains: "psycopg2.connect"
    - path: "fee_crawler/modal_app.py"
      provides: "Quarterly date gate for FFIEC/NCUA ingestion"
      contains: "quarterly"
  key_links:
    - from: "fee_crawler/commands/ingest_call_reports.py"
      to: "institution_financials table"
      via: "psycopg2 INSERT ON CONFLICT"
      pattern: "ON CONFLICT.*crawl_target_id.*report_date.*source"
    - from: "fee_crawler/commands/ingest_ncua.py"
      to: "institution_financials table"
      via: "psycopg2 INSERT ON CONFLICT"
      pattern: "ON CONFLICT.*crawl_target_id.*report_date.*source"
    - from: "fee_crawler/modal_app.py"
      to: "fee_crawler/commands/ingest_call_reports.py"
      via: "subprocess CLI call with quarterly date gate"
      pattern: "ingest-call-reports"
---

<objective>
Port FFIEC CDR and NCUA 5300 ingestion scripts from SQLite to Postgres, add historical backfill capability (2010-present), add retry/error handling, and wire quarterly auto-ingestion into the Modal cron.

Purpose: Populate the `institution_financials` Postgres table with quarterly Call Report data for all matched institutions, enabling the institution detail page to show real financial profiles.

Output: Two Postgres-native ingestion CLI commands with `--backfill` mode + quarterly auto-ingestion in Modal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-ffiec-pipeline-institution-data/58-CONTEXT.md
@.planning/phases/58-ffiec-pipeline-institution-data/58-RESEARCH.md

@fee_crawler/commands/ingest_call_reports.py
@fee_crawler/commands/ingest_ncua.py
@fee_crawler/modal_app.py
@fee_crawler/__main__.py
@fee_crawler/db.py

<interfaces>
<!-- Existing institution_financials schema (from scripts/migrate-schema.sql): -->
CREATE TABLE IF NOT EXISTS institution_financials (
    id                      BIGSERIAL PRIMARY KEY,
    crawl_target_id         BIGINT NOT NULL REFERENCES crawl_targets(id),
    report_date             TEXT NOT NULL,
    source                  TEXT NOT NULL,
    total_assets            BIGINT,
    total_deposits          BIGINT,
    total_loans             BIGINT,
    service_charge_income   BIGINT,
    other_noninterest_income BIGINT,
    net_interest_margin     FLOAT,
    efficiency_ratio        FLOAT,
    roa                     FLOAT,
    roe                     FLOAT,
    tier1_capital_ratio     FLOAT,
    branch_count            INT,
    employee_count          INT,
    member_count            INT,
    raw_json                JSONB,
    fetched_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    total_revenue           BIGINT,
    fee_income_ratio        FLOAT,
    UNIQUE(crawl_target_id, report_date, source)
);

<!-- Existing CLI registration pattern (from __main__.py): -->
ingest-call-reports subparser with --csv, --report-date, --source args
ingest-ncua subparser with --quarter, --limit args

<!-- Existing Modal ingest_data (modal_app.py lines 145-167): -->
ingest_data() runs daily at 10am UTC.
  - Daily: ingest-fred, ingest-nyfed, ingest-bls, ingest-ofr
  - Weekly (Monday): ingest-fdic, ingest-ncua, ingest-cfpb, ingest-sod, ingest-beige-book, ingest-call-reports, ingest-census-acs

<!-- Scaling conventions: -->
FFIEC RIAD fields: thousands of dollars (multiply by 1000 via _apply_ffiec_scaling)
NCUA ACCT fields: whole dollars (divide by 1000 in existing code to normalize to thousands)
DB stores: thousands of dollars for all monetary fields
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port ingest_call_reports.py and ingest_ncua.py to Postgres with backfill + retry</name>
  <files>
    fee_crawler/commands/ingest_call_reports.py
    fee_crawler/commands/ingest_ncua.py
    fee_crawler/__main__.py
  </files>
  <read_first>
    fee_crawler/commands/ingest_call_reports.py
    fee_crawler/commands/ingest_ncua.py
    fee_crawler/__main__.py
    fee_crawler/db.py
    fee_crawler/commands/ingest_fdic.py
    scripts/migrate-schema.sql
  </read_first>
  <action>
**Port ingest_call_reports.py (per D-01):**
1. Replace `from fee_crawler.db import Database` with `import os, psycopg2, psycopg2.extras`.
2. Replace `db: Database` parameter with `conn = psycopg2.connect(os.environ["DATABASE_URL"])` at the start of the `run()` function. Use `conn.autocommit = False` and `cur = conn.cursor(cursor_factory=psycopg2.extras.RealDictCursor)`.
3. Replace all `db.execute(sql, params)` with `cur.execute(sql, params)` and `?` placeholders with `%s`.
4. Replace `db.commit()` with `conn.commit()`.
5. Replace SQLite `datetime('now')` with `NOW()`.
6. Keep `_apply_ffiec_scaling()` exactly as-is (per D-01: "keep existing field mappings and parsing logic").
7. Keep `_SERVICE_CHARGE_CODES`, `_FFIEC_BULK_URL`, and `_ingest_from_csv()` logic intact.
8. Use `ON CONFLICT (crawl_target_id, report_date, source) DO UPDATE SET` for upserts.
9. For institution matching (per D-09): match by cert_number first (`SELECT id FROM crawl_targets WHERE cert_number = %s`), then fuzzy name+state (`WHERE LOWER(institution_name) = LOWER(%s) AND state_code = %s`). Log match type.
10. For unmatched institutions (per D-10): log a count of skipped unmatched rows but do not error. The `crawl_target_id NOT NULL` constraint means unmatched rows are skipped for now.

**Add backfill mode (per D-02, D-03):**
11. Add `--backfill` flag and `--from-year` arg (default 2010) to the CLI parser in `__main__.py`.
12. When `--backfill` is set, loop through quarters from `from_year` to current year. For each quarter, attempt to download the FFIEC bulk CSV (use FDIC BankFind API as recommended in RESEARCH.md, pattern from `ingest_fdic.py`). Commit after each quarter to avoid losing progress on timeout.
13. If FDIC BankFind does not provide income fields, fall back to reading local CSV files if provided via `--csv`.

**Add retry + error handling (per D-08):**
14. Wrap HTTP downloads in a retry loop: 3 attempts, exponential backoff (2s, 4s, 8s). Use `requests.Session` with timeout=60.
15. On all 3 retries failing, log error with quarter details and continue to next quarter (do not abort).
16. Print a summary report at end: quarters processed, rows upserted, rows skipped, errors.

**Port ingest_ncua.py (per D-01):**
17. Same psycopg2 swap: replace `Database`/`?` with `psycopg2.connect(DATABASE_URL)`/`%s`.
18. Keep all `FS220_FIELDS` and `FS220A_FIELDS` mappings exactly as-is.
19. Keep `_safe_int`, `_safe_float`, `_parse_cycle_date` helpers.
20. Replace `db.execute`/`db.commit` with `cur.execute`/`conn.commit`.
21. Add `--backfill` and `--from-year` flags. When set, iterate quarters (3, 6, 9, 12) from `from_year` to present using `NCUA_ZIP_BASE.format(year=year, month=month)`.
22. Add same retry logic (3 attempts, exponential backoff) for ZIP downloads.
23. For NCUA matching (per D-09): match by charter_number (`SELECT id FROM crawl_targets WHERE cert_number = %s AND charter_type = 'credit_union'`). Skip unmatched.

**Update __main__.py:**
24. Add `--backfill` (store_true) and `--from-year` (int, default=2010) args to both the `ingest-call-reports` and `ingest-ncua` subparsers.
25. Pass these args through to the respective run functions.
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && python -c "from fee_crawler.commands.ingest_call_reports import run; print('FFIEC import OK')" && python -c "from fee_crawler.commands.ingest_ncua import ingest_ncua_financials; print('NCUA import OK')" && python -m fee_crawler ingest-call-reports --help | grep -q 'backfill' && echo "BACKFILL_FLAG_OK" && python -m fee_crawler ingest-ncua --help | grep -q 'backfill' && echo "NCUA_BACKFILL_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "psycopg2.connect" fee_crawler/commands/ingest_call_reports.py` succeeds (Postgres connection)
    - `grep -q "psycopg2.connect" fee_crawler/commands/ingest_ncua.py` succeeds (Postgres connection)
    - `grep -q "ON CONFLICT" fee_crawler/commands/ingest_call_reports.py` succeeds (upsert pattern)
    - `grep -q "ON CONFLICT" fee_crawler/commands/ingest_ncua.py` succeeds (upsert pattern)
    - `grep -q "backfill" fee_crawler/commands/ingest_call_reports.py` succeeds (backfill flag)
    - `grep -q "backfill" fee_crawler/commands/ingest_ncua.py` succeeds (backfill flag)
    - `grep -q "retry\|retries\|attempt" fee_crawler/commands/ingest_call_reports.py` succeeds (retry logic)
    - `grep -q "retry\|retries\|attempt" fee_crawler/commands/ingest_ncua.py` succeeds (retry logic)
    - `grep -c "from fee_crawler.db import Database" fee_crawler/commands/ingest_call_reports.py` returns 0 (no SQLite import)
    - `grep -c "from fee_crawler.db import Database" fee_crawler/commands/ingest_ncua.py` returns 0 (no SQLite import)
    - `python -m fee_crawler ingest-call-reports --help` shows `--backfill` and `--from-year` flags
    - `python -m fee_crawler ingest-ncua --help` shows `--backfill` and `--from-year` flags
  </acceptance_criteria>
  <done>
    Both ingestion scripts use psycopg2 for Postgres, support --backfill --from-year for historical loading, retry failed HTTP downloads 3 times with exponential backoff, match institutions by cert/charter number first then fuzzy name+state, log unmatched rows and continue, and print summary reports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add quarterly date gate to Modal ingest_data for FFIEC/NCUA</name>
  <files>
    fee_crawler/modal_app.py
  </files>
  <read_first>
    fee_crawler/modal_app.py
  </read_first>
  <action>
**Wire quarterly ingestion into existing Modal cron (per D-07, respecting Pitfall 3: no new cron):**

1. In the `ingest_data()` function (line ~145), after the weekly Monday block, add a quarterly block:
```python
# Quarterly (Feb 15, May 15, Aug 15, Nov 15): FFIEC + NCUA full ingest
now = datetime.now(timezone.utc)
is_quarterly = now.month in (2, 5, 8, 11) and now.day == 15
if is_quarterly:
    for cmd in ["ingest-call-reports", "ingest-ncua"]:
        r = subprocess.run(["python3", "-m", "fee_crawler", cmd],
                           capture_output=True, text=True, env=env,
                           timeout=3600)
        results.append(f"quarterly-{cmd}: {'OK' if r.returncode == 0 else 'FAIL'}")
```

2. The quarterly gate runs the standard (non-backfill) ingestion on the approximate FFIEC release dates. This fetches the latest available quarter data.
3. Do NOT add a new `@app.function(schedule=...)` — this stays inside the existing `ingest_data` function to respect the 5-cron limit.
4. Add a `timeout=3600` to the subprocess calls for quarterly ingestion (they download larger files than daily ingestion).
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && grep -q "is_quarterly" fee_crawler/modal_app.py && echo "QUARTERLY_GATE_OK" && grep -q "month in (2, 5, 8, 11)" fee_crawler/modal_app.py && echo "MONTHS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "is_quarterly" fee_crawler/modal_app.py` succeeds
    - `grep -q "month in (2, 5, 8, 11)" fee_crawler/modal_app.py` succeeds
    - `grep -q "day == 15" fee_crawler/modal_app.py` succeeds
    - `grep -c 'schedule=modal.Cron' fee_crawler/modal_app.py` returns exactly 5 (no new cron added)
    - `grep -q "quarterly.*ingest-call-reports" fee_crawler/modal_app.py` succeeds
  </acceptance_criteria>
  <done>
    Modal ingest_data function includes a quarterly date gate that runs FFIEC and NCUA ingestion on Feb/May/Aug/Nov 15th. No new cron job added. Subprocess timeout set to 3600s for quarterly runs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External API to pipeline | FFIEC/NCUA/FDIC data downloaded over HTTPS into Python pipeline |
| Pipeline to Postgres | Python pipeline writes to institution_financials via psycopg2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | ingest_call_reports.py SQL | mitigate | All SQL uses `%s` parameterized queries via psycopg2 -- no string interpolation for any field values |
| T-58-02 | Tampering | ingest_ncua.py SQL | mitigate | Same `%s` parameterized queries pattern -- cert_number, report_date, amounts all parameterized |
| T-58-03 | Information Disclosure | DATABASE_URL in env | mitigate | DATABASE_URL read from os.environ (Modal secrets or .env.local) -- never logged or printed |
| T-58-04 | Denial of Service | Backfill downloading 60+ quarters | accept | Backfill is manual CLI only, not cron -- operator controls timing and can Ctrl+C |
| T-58-05 | Spoofing | FDIC/NCUA data integrity | accept | Data comes from government HTTPS endpoints -- no additional signature verification needed for public financial data |
</threat_model>

<verification>
1. `python -m fee_crawler ingest-call-reports --help` shows --backfill, --from-year
2. `python -m fee_crawler ingest-ncua --help` shows --backfill, --from-year
3. Both scripts import psycopg2, not Database from fee_crawler.db
4. Both scripts use ON CONFLICT upserts
5. Modal ingest_data has quarterly gate without new cron
6. No console.log or debug print statements left in production paths
</verification>

<success_criteria>
- FFIEC CDR ingestion script connects to Postgres, upserts financial rows with proper scaling
- NCUA 5300 ingestion script connects to Postgres, upserts financial rows from ZIP downloads
- Both scripts support --backfill --from-year for historical loading from 2010
- HTTP download failures retry 3 times with exponential backoff
- Modal quarterly gate triggers on Feb/May/Aug/Nov 15th without adding a 6th cron
- All SQL uses parameterized queries (no string interpolation)
</success_criteria>

<output>
After completion, create `.planning/phases/58-ffiec-pipeline-institution-data/58-01-SUMMARY.md`
</output>
