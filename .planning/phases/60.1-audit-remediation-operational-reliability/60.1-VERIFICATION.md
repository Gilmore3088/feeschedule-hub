---
phase: 60.1-audit-remediation-operational-reliability
verified: 2026-04-15T14:36:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 60.1: Audit Remediation — Operational Reliability Pass — Verification Report

**Phase Goal:** The repo audit findings (P0-P2) are resolved, Phase 59 test regressions are fixed, and the FFIEC scaling contradiction between TS query layer and Python ingest is reconciled — automated jobs fail loudly, schemas agree, and stuck report jobs transition to failed.
**Verified:** 2026-04-15T14:36:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Modal scheduled jobs fail loudly on subprocess errors via shared helper | VERIFIED | `SubprocessFailed` + `run_checked` in `modal_app.py` lines 16-53; all four scheduled functions route through it |
| 2 | One authoritative schema source for pipeline tables (SQLite retired with clear error) | VERIFIED | `require_postgres()` in `db.py` lines 38-57; all four entrypoints call it at first statement |
| 3 | Report jobs transition to `failed` when Modal triggering fails | VERIFIED | `markJobFailed()` in `route.ts` lines 34-47; three failure paths all call it |
| 4 | CI and Modal install the same Python dependency set | VERIFIED | CI uses `fee_crawler/requirements.txt`; root `requirements.txt` is `-r fee_crawler/requirements.txt` redirect |
| 5 | FFIEC scaling contract consistent — helper restored, multiply-up direction | VERIFIED | `_apply_ffiec_scaling` at lines 55-71 of `ingest_call_reports.py`; multiplies by 1000 for source=='ffiec' |
| 6 | Phase 59 regressions pass — `test_stealth_fetcher.py` and `test_call_report_scaling.py` collect and pass | VERIFIED | `Stealth` at module scope in `playwright_fetcher.py` line 30-32; both test files present; pytest: 250 passed, 0 failed |
| 7 | `run_monthly_pulse` uses `BFI_APP_URL` and is documented manual-only | VERIFIED | `BFI_APP_URL` at line 462 of `modal_app.py`; docstring at lines 445-455 explicitly states "Manual-only trigger" and "NOT scheduled" |

**Score:** 7/7 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `fee_crawler/modal_app.py` | `SubprocessFailed` class + `run_checked` helper | VERIFIED | Lines 16-53; all scheduled fns use `run_checked` |
| `fee_crawler/db.py` | `require_postgres(reason)` helper | VERIFIED | Lines 38-57; raises RuntimeError when `DATABASE_URL` unset |
| `fee_crawler/modal_preflight.py` | Calls `require_postgres` | VERIFIED | `preflight_postgres()` calls it at line 225 |
| `fee_crawler/workers/discovery_worker.py` | Calls `require_postgres()` as first statement | VERIFIED | Line 327 in `run()` function |
| `fee_crawler/workers/data_integrity.py` | Calls `require_postgres()` as first statement | VERIFIED | Line 21 in `run_checks()` |
| `fee_crawler/workers/daily_report.py` | Calls `require_postgres()` as first statement | VERIFIED | Line 22 in `generate_report()` |
| `fee_crawler/commands/crawl.py` | Calls `require_postgres()` as first statement | VERIFIED | Lines 461-464 in `run()` |
| `src/app/api/reports/generate/route.ts` | `markJobFailed` + three failure paths | VERIFIED | Lines 34-47 helper; missing URL (line 159), non-OK (line 180), thrown error (line 184-185) |
| `.github/workflows/e2e-tests.yml` | Installs `fee_crawler/requirements.txt` | VERIFIED | Both e2e-fast and e2e-full steps use `pip install -r fee_crawler/requirements.txt` |
| `requirements.txt` (root) | `-r fee_crawler/requirements.txt` redirect or deleted | VERIFIED | Single line: `-r fee_crawler/requirements.txt` |
| `fee_crawler/commands/ingest_call_reports.py` | `_apply_ffiec_scaling` helper, multiply direction | VERIFIED | Lines 55-71; `source == 'ffiec'` multiplies by 1000 |
| `fee_crawler/tests/test_call_report_scaling.py` | Imports helper, tests multiply direction | VERIFIED | All 8 test cases present; imports `_apply_ffiec_scaling` directly |
| `fee_crawler/pipeline/playwright_fetcher.py` | `Stealth` at module scope | VERIFIED | Lines 29-32: try/except import, `Stealth = None` fallback |
| `fee_crawler/tests/test_stealth_fetcher.py` | Patches module-level `Stealth` symbol | VERIFIED | Tests patch `fee_crawler.pipeline.playwright_fetcher.Stealth` |
| `fee_crawler/tests/test_modal_subprocess_helper.py` | Tests for `run_checked` + `SubprocessFailed` | VERIFIED | 7 test cases covering success, failure, tailing, cwd/env forwarding |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `run_pdf_extraction()` | `run_checked` | direct call | WIRED | `modal_app.py` line 115 |
| `run_browser_extraction()` | `run_checked` | direct call | WIRED | `modal_app.py` line 135 |
| `run_post_processing()` | `run_checked` (per command) | loop | WIRED | `modal_app.py` lines 159-162 |
| `ingest_data()` | `run_checked` (per ingestor) | try/except loop | WIRED | `modal_app.py` lines 193-220 |
| `markJobFailed` | `report_jobs` table | `UPDATE ... SET status='failed'` | WIRED | `route.ts` lines 37-44 |
| Missing `MODAL_REPORT_URL` | 503 + failed status | `markJobFailed` call | WIRED | `route.ts` lines 156-163 |
| Non-OK Modal response | failed status | `markJobFailed(jobId, ...)` | WIRED | `route.ts` line 180 |
| Thrown fetch error | failed status | `markJobFailed(jobId, reason)` | WIRED | `route.ts` lines 183-185 |
| `_apply_ffiec_scaling` | call site in `run()` | direct call at line 320 | WIRED | `ingest_call_reports.py` line 320 |
| `Stealth` import | module scope | try/except at module level | WIRED | `playwright_fetcher.py` lines 29-32 |
| `run_monthly_pulse` | `BFI_APP_URL` | `os.environ.get("BFI_APP_URL")` | WIRED | `modal_app.py` line 462 |

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| pytest unit suite (not e2e/llm/slow) | `pytest -m "not e2e and not llm and not slow" -q` | 250 passed, 30 deselected in 0.69s | PASS |
| vitest TypeScript suite | `npx vitest run` | 387 passed, 1 skipped (388) | PASS |
| `Stealth` at module scope | `python -c "from fee_crawler.pipeline import playwright_fetcher; assert hasattr(playwright_fetcher, 'Stealth'); print('OK')"` | OK | PASS |
| `NEXT_PUBLIC_APP_URL` absent from `modal_app.py` | `grep -n "NEXT_PUBLIC_APP_URL" fee_crawler/modal_app.py` | empty (exit 1) | PASS |

---

## Requirements Coverage

| Requirement | Plan | Status | Evidence |
|-------------|------|--------|----------|
| OPS-01 (Modal subprocess hardening) | 60.1-01 | SATISFIED | `SubprocessFailed` + `run_checked` in `modal_app.py`; 4 scheduled fns use it |
| OPS-02 (Schema authority) | 60.1-02 | SATISFIED | `require_postgres()` in `db.py`; 4 entrypoints call it at function entry |
| OPS-03 (Report job failure propagation) | 60.1-03 | SATISFIED | `markJobFailed()` covers all 3 failure paths in `route.ts` |
| OPS-04 (CI/Modal dep parity) | 60.1-04 | SATISFIED | CI installs `fee_crawler/requirements.txt`; root `requirements.txt` is a redirect |
| OPS-05 (FFIEC scaling contract) | 60.1-05 | SATISFIED | `_apply_ffiec_scaling` restored; multiply direction consistent with migration 023 and tests |
| OPS-06 (Phase 59 regressions) | 60.1-06 | SATISFIED | `Stealth` at module scope; both test files collect and pass in 250-test run |
| OPS-07 (monthly_pulse config) | 60.1-07 | SATISFIED | `BFI_APP_URL` used; docstring says "Manual-only trigger, NOT scheduled" |

---

## Anti-Patterns Found

None that affect goal achievement.

- `fee_crawler/modal_app.py` `ingest_data()` wraps per-ingestor failures in try/except and re-raises as `SubprocessFailed` at the end if any failed — this is intentional opt-in degradation with explicit failure tracking (compliant with SC1 "named flag" opt-in).
- `preflight_e2e()` in `modal_preflight.py` deliberately pops `DATABASE_URL` for isolation — this is documented and intentional, not a silent SQLite fallback.

---

## Human Verification Required

None. All success criteria were verifiable programmatically through code inspection and test execution.

---

## Gaps Summary

No gaps. All 7 success criteria are met:

1. `SubprocessFailed` + `run_checked` in `modal_app.py` — all four cron-scheduled functions route subprocess calls through it, raising with stdout/stderr tails on non-zero exit.
2. `require_postgres()` in `db.py` — called at the entry of all four pipeline entrypoints (`crawl.run`, `discovery_worker.run`, `data_integrity.run_checks`, `daily_report.generate_report`). `modal_preflight.py` imports and calls it.
3. `markJobFailed()` in `route.ts` covers missing `MODAL_REPORT_URL` (503 + immediate fail), non-OK Modal response, and thrown fetch errors — no job can remain permanently pending.
4. Both CI jobs install `fee_crawler/requirements.txt` directly; root `requirements.txt` is a single `-r` redirect with no divergence risk.
5. `_apply_ffiec_scaling` is present at lines 55-71 of `ingest_call_reports.py`, multiplies by 1000 for `source == 'ffiec'`, and is called at line 320 inside the ingest loop. `test_call_report_scaling.py` imports and tests the same helper.
6. `pytest -m "not e2e and not llm and not slow" -q` exits green: 250 passed, 0 failed. `Stealth` is exported at module scope in `playwright_fetcher.py`.
7. `run_monthly_pulse` reads `BFI_APP_URL` (not `NEXT_PUBLIC_APP_URL`); its docstring explicitly states "Manual-only trigger. NOT scheduled."

**Corrective commit `05af940` for SC5 confirmed effective:** `_apply_ffiec_scaling` exists at lines 55-71, is called at line 320, and all 8 scaling tests pass.

---

_Verified: 2026-04-15T14:36:00Z_
_Verifier: Claude (gsd-verifier)_
