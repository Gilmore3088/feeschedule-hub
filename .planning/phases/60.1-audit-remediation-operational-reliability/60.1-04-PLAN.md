---
phase: 60.1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/e2e-tests.yml
  - requirements.txt
autonomous: true
requirements: [OPS-04]
must_haves:
  truths:
    - "CI installs the same Python dependency set Modal uses"
    - "Root requirements.txt either references fee_crawler/requirements.txt or is removed"
    - "No 'green CI, broken Modal' from manifest drift"
  artifacts:
    - path: ".github/workflows/e2e-tests.yml"
      provides: "Both jobs install fee_crawler/requirements.txt"
      contains: "fee_crawler/requirements.txt"
  key_links:
    - from: ".github/workflows/e2e-tests.yml"
      to: "fee_crawler/requirements.txt"
      via: "pip install -r"
      pattern: "fee_crawler/requirements.txt"
---

<objective>
Resolve audit Finding 4 (P1): CI installs root `requirements.txt`; Modal images install `fee_crawler/requirements.txt`. The manifests differ materially (Modal includes psycopg2-binary, asyncpg, httpx, anthropic, playwright, playwright-stealth, boto3 — root does not). Unify on the Modal manifest.

Purpose: CI must validate the deployed environment.

Output: e2e-tests.yml installs the Modal manifest; root requirements.txt either redirects via `-r` or is deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60.1-audit-remediation-operational-reliability/60.1-CONTEXT.md
@docs/audits/repo-audit-2026-04-15.md
@.github/workflows/e2e-tests.yml
@fee_crawler/requirements.txt
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify CI and root requirements on the Modal manifest</name>
  <files>.github/workflows/e2e-tests.yml, requirements.txt</files>
  <read_first>
    - .github/workflows/e2e-tests.yml (full file — both jobs e2e-fast and e2e-full)
    - fee_crawler/requirements.txt (the canonical Modal manifest)
    - requirements.txt (current root file)
    - Run a quick diff in your head: which packages are unique to root? If any, they need to be preserved by adding to fee_crawler/requirements.txt — but per CONTEXT.md "Prefer option (a)" only retain root if it has non-crawler consumers.
  </read_first>
  <action>
    1. In `.github/workflows/e2e-tests.yml`, change BOTH "Install Python dependencies" steps (lines ~45-46 and ~88-89):
       ```yaml
       - name: Install Python dependencies
         run: pip install -r fee_crawler/requirements.txt pytest pytest-timeout
       ```

    2. Inspect `requirements.txt` against `fee_crawler/requirements.txt`:
       - If root contains ONLY packages that are also in fee_crawler/requirements.txt (or strict subset), rewrite root `requirements.txt` to a single line: `-r fee_crawler/requirements.txt`. This keeps `pip install -r requirements.txt` working from the repo root for any developer muscle memory or external tooling.
       - If root has packages NOT in the Modal manifest, FIRST add them to `fee_crawler/requirements.txt` (preserving the audit's intent that Modal is the authoritative manifest), THEN rewrite root to `-r fee_crawler/requirements.txt`.

    3. Do NOT delete `requirements.txt` even if it becomes a one-line redirect — the redirect is the safer migration. (The CONTEXT.md "delete or retain" decision: retain as redirect for safety.)

    4. Do NOT change pytest invocation, system deps, environment vars, or any other workflow steps.
  </action>
  <verify>
    <automated>grep -c "fee_crawler/requirements.txt" .github/workflows/e2e-tests.yml | awk '{ if ($1 < 2) exit 1 }' && grep -q "fee_crawler/requirements.txt" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fee_crawler/requirements.txt" .github/workflows/e2e-tests.yml` returns 2 (one per job)
    - `! grep -E "^pip install -r requirements.txt " .github/workflows/e2e-tests.yml` (the bare root reference is gone)
    - `head -1 requirements.txt` contains `-r fee_crawler/requirements.txt` OR `requirements.txt` no longer exists
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/e2e-tests.yml'))"` exits 0 (YAML still valid)
  </acceptance_criteria>
  <done>Both CI jobs install the Modal manifest; root requirements.txt is a redirect (or contains the same content).</done>
</task>

</tasks>

<verification>
- YAML parses cleanly
- Both job blocks reference fee_crawler/requirements.txt
- No bare root requirements.txt install remains in CI
</verification>

<success_criteria>
CI and Modal install the same Python dependency set. Manifest drift is structurally impossible going forward (single source of truth via -r).
</success_criteria>

<output>
Create `.planning/phases/60.1-audit-remediation-operational-reliability/60.1-04-SUMMARY.md`
</output>
