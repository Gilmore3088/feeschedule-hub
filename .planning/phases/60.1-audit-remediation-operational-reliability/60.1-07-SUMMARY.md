---
phase: 60.1-audit-remediation-operational-reliability
plan: 07
subsystem: infra
tags: [modal, python, fee-crawler, env-vars, convention]

# Dependency graph
requires: [60.1-01]
provides:
  - "run_monthly_pulse reads BFI_APP_URL matching the rest of the report stack"
  - "run_monthly_pulse docstring states manual-only with cron slot explanation"
affects: [modal-ops, fee-crawler-pipeline]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "BFI_APP_URL: canonical env var for all report-triggering code paths (fee_crawler + src/app/api/reports/*)"

key-files:
  created: []
  modified:
    - fee_crawler/modal_app.py

key-decisions:
  - "Chose BFI_APP_URL over NEXT_PUBLIC_APP_URL to match src/app/api/reports/* convention — single canonical env var for the report stack"
  - "Documented run_monthly_pulse as manual-only with cron slot reasoning inline — no schedule= argument added"

requirements-completed: [OPS-07]

# Metrics
duration: 5min
completed: 2026-04-15
---

# Phase 60.1 Plan 07: Monthly Pulse Env-Var Convention Fix Summary

**run_monthly_pulse pinned to BFI_APP_URL with explicit manual-only docstring — eliminates the NEXT_PUBLIC_APP_URL inconsistency flagged in audit Finding 6**

## Performance

- **Duration:** 5 min
- **Completed:** 2026-04-15
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Replaced `os.environ.get("NEXT_PUBLIC_APP_URL", "")` with `os.environ.get("BFI_APP_URL", "")` in `run_monthly_pulse`
- Updated early-return error string from `"NEXT_PUBLIC_APP_URL not set"` to `"BFI_APP_URL not set"` for consistency
- Replaced misleading scheduled-job docstring with a manual-only docstring that explains the cron slot exhaustion rationale and the correct invocation command
- Confirmed no `schedule=` argument is present on the `@app.function` decorator for `run_monthly_pulse`
- Confirmed zero remaining `NEXT_PUBLIC_APP_URL` references in `fee_crawler/modal_app.py`
- Module imports cleanly (`python -c "import fee_crawler.modal_app"` exits 0)
- Plan 60.1-01 work (`run_checked`, `SubprocessFailed`, refactored scheduled functions) is untouched

## Task Commits

1. **Task 1: Pin run_monthly_pulse to BFI_APP_URL and document manual-only** - `6f11fac`

## Files Modified

- `fee_crawler/modal_app.py` — `run_monthly_pulse` docstring, env var reference, error string

## Decisions Made

- `BFI_APP_URL` chosen: it is the env var used by `src/app/api/reports/*` (the server-side report stack), making the crawler and API consistent. `NEXT_PUBLIC_APP_URL` is a client-side Next.js convention that has no place in a Modal worker.
- Docstring documents manual-only status with the exact `modal run` invocation and the reason (cron slot budget exhausted by the five scheduled functions), so the next engineer doesn't inadvertently add a schedule.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None - no new network endpoints or auth paths introduced. Only an env var reference changed.

## Self-Check: PASSED

- `fee_crawler/modal_app.py` — exists and contains `BFI_APP_URL`, `Manual-only trigger`, `BFI_APP_URL not set`
- `grep -n "NEXT_PUBLIC_APP_URL" fee_crawler/modal_app.py` — returns no matches (exit 1)
- `grep -n "schedule=" fee_crawler/modal_app.py` — no matches on lines 373-378 (run_monthly_pulse decorator)
- `python -c "import fee_crawler.modal_app"` — exits 0
- Commit `6f11fac` — task commit

---
*Phase: 60.1-audit-remediation-operational-reliability*
*Completed: 2026-04-15*
