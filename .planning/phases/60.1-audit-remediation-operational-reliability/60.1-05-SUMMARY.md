---
phase: 60.1
plan: "05"
subsystem: fee-crawler
tags: [ffiec, call-reports, scaling, data-integrity, audit-remediation]
dependency_graph:
  requires: []
  provides: [ffiec-scaling-contract-consistent]
  affects: [fee_crawler/commands/ingest_call_reports.py, scripts/migrations/023-fix-ffiec-scaling.sql]
tech_stack:
  added: []
  patterns: [single-helper-for-unit-conversion, migration-cross-reference-comments]
key_files:
  created: []
  modified:
    - scripts/migrations/023-fix-ffiec-scaling.sql
decisions:
  - "FFIEC scaling contract: helper already correct in worktree branch; only migration cross-reference comment was missing"
  - "Adopted existing tuple-based _apply_ffiec_scaling(source, sc, oni) signature (not single-value scalar); tests confirmed this is canonical"
  - "No changes to ingest_call_reports.py — helper and call sites were already correct"
metrics:
  duration: "8 minutes"
  completed: "2026-04-15"
  tasks_completed: 2
  files_modified: 1
---

# Phase 60.1 Plan 05: FFIEC Scaling Contract Alignment Summary

FFIEC Call Report scaling contract confirmed consistent — `_apply_ffiec_scaling` helper already existed and was correct; migration 023 cross-referenced to close audit Finding 5.

## What Was Done

### Task 1: Verify and confirm _apply_ffiec_scaling helper

Upon reading `fee_crawler/commands/ingest_call_reports.py`, the `_apply_ffiec_scaling` helper was already present and correct at lines 47-63 with the canonical signature:

```python
def _apply_ffiec_scaling(source, service_charges, other_noninterest):
```

The helper correctly multiplies by 1000 for `source == "ffiec"` and passes NCUA values through unchanged. It was already called at the correct call site (line 120-122) for both `service_charges` and `other_noninterest`. No bare `// 1000` or `/ 1000` divisions existed anywhere in the file.

All 8 tests in `fee_crawler/tests/test_call_report_scaling.py` were already passing before any changes were made. The plan's description of lines 296-299 with a buggy `sc_raw // 1000` pattern does not exist in this worktree branch.

**Conclusion:** Task 1 was already complete. No changes to `ingest_call_reports.py` or `test_call_report_scaling.py` were needed.

### Task 2: Add migration 023 cross-reference comment

Added 2-line cross-reference to `scripts/migrations/023-fix-ffiec-scaling.sql` immediately after the existing header block:

```sql
-- Phase 60.1 (2026-04-15): contract reaffirmed. Ingest helper restored.
-- See fee_crawler/commands/ingest_call_reports.py::_apply_ffiec_scaling.
```

No UPDATE statements, DDL, or idempotency guards were modified.

## Verification

```
pytest fee_crawler/tests/test_call_report_scaling.py -q
8 passed in 0.10s

grep -q "def _apply_ffiec_scaling" fee_crawler/commands/ingest_call_reports.py  → PASS
grep -q "Phase 60.1" scripts/migrations/023-fix-ffiec-scaling.sql              → PASS
grep -q "_apply_ffiec_scaling" scripts/migrations/023-fix-ffiec-scaling.sql    → PASS
git diff scripts/migrations/023-fix-ffiec-scaling.sql → comment-only (3 lines added)
```

Import and behavior probe:
```
python -c "from fee_crawler.commands.ingest_call_reports import _apply_ffiec_scaling; ..."
OK - import and behavior correct
```

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| Task 1 (no-op) | — | _apply_ffiec_scaling already correct; no changes needed |
| Task 2 | 6ba9f30 | fix(60.1-05): align migration 023 docs with canonical FFIEC scaling contract |

## Deviations from Plan

### Plan Deviation: Ingest file was already correct

**Found during:** Task 1 pre-work (reading the target file)

**Issue:** The plan described a bug at lines 296-299 of `ingest_call_reports.py` where SC was being divided by 1000. The actual file in this worktree branch has only 265 lines and contains no such bug. The `_apply_ffiec_scaling` helper already existed, had the correct multiply-by-1000 direction, and was already wired to the call site.

**Disposition:** No fix needed. Tests confirmed correct behavior. Proceeded to Task 2 only.

**Impact:** 1 file modified instead of 2.

### Plan Deviation: Helper signature differs from plan spec

**Found during:** Task 1 analysis

**Issue:** The plan specified a single-value scalar helper `_apply_ffiec_scaling(value) -> value * 1000`, but the existing helper is `_apply_ffiec_scaling(source, sc, oni) -> tuple`. The test file was written against the tuple-based API, which is more expressive (handles NCUA passthrough by source type).

**Disposition:** Accepted existing signature as canonical. The tests all pass; the contract is correct. Changing to a single-value API would require rewriting both the ingest code and all 8 tests for no functional gain.

**Rule applied:** Rule 1 (auto-fix) does not apply — no bug exists. Kept existing correct implementation.

## Known Stubs

None.

## Threat Flags

None. This change is comment-only in the migration file. No new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

- [x] `fee_crawler/commands/ingest_call_reports.py` — confirmed correct, no changes
- [x] `scripts/migrations/023-fix-ffiec-scaling.sql` — cross-reference added, commit 6ba9f30 exists
- [x] `pytest fee_crawler/tests/test_call_report_scaling.py -q` — 8 passed
- [x] All acceptance criteria met
