---
phase: 60.1
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - fee_crawler/pipeline/playwright_fetcher.py
autonomous: true
requirements: [OPS-04]
must_haves:
  truths:
    - "fee_crawler.pipeline.playwright_fetcher.Stealth resolves at module scope"
    - "fee_crawler/tests/test_stealth_fetcher.py collects and passes"
    - "Existing stealth-fetch runtime behavior is preserved"
  artifacts:
    - path: "fee_crawler/pipeline/playwright_fetcher.py"
      provides: "Module-level Stealth import (with graceful fallback when playwright_stealth is missing)"
      contains: "Stealth"
  key_links:
    - from: "fee_crawler/tests/test_stealth_fetcher.py"
      to: "fee_crawler.pipeline.playwright_fetcher.Stealth"
      via: "unittest.mock.patch target"
      pattern: "playwright_fetcher.Stealth"
---

<objective>
Resolve the Phase 59 test regression in `fee_crawler/tests/test_stealth_fetcher.py`. The test patches `fee_crawler.pipeline.playwright_fetcher.Stealth`, but `Stealth` is currently imported INSIDE a function (line 364), so the patch target does not exist at module scope and the test fails with AttributeError.

Purpose: Restore module-level `Stealth` symbol so the existing patch target resolves, without changing runtime fetch behavior.

Output: Stealth is importable at module scope (with safe fallback) and the test passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60.1-audit-remediation-operational-reliability/60.1-CONTEXT.md
@docs/audits/repo-audit-2026-04-15.md
@fee_crawler/pipeline/playwright_fetcher.py
@fee_crawler/tests/test_stealth_fetcher.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist Stealth import to module scope with fallback</name>
  <files>fee_crawler/pipeline/playwright_fetcher.py</files>
  <read_first>
    - fee_crawler/pipeline/playwright_fetcher.py (full file — focus on the imports block at top, and lines around 360-370 where Stealth is currently imported inside a function)
    - fee_crawler/tests/test_stealth_fetcher.py (full file — confirm the exact patch target string used in @patch / mock.patch decorators)
  </read_first>
  <action>
    1. In `fee_crawler/pipeline/playwright_fetcher.py`, add to the top-level imports section (after the existing `from __future__` line and around the other top imports):
       ```python
       try:
           from playwright_stealth import Stealth  # noqa: F401  (used at runtime + patched in tests)
       except ImportError:  # pragma: no cover
           Stealth = None
       ```

    2. At the existing in-function import site (around line 364):
       ```python
       from playwright_stealth import Stealth
       _stealth = Stealth()
       ```
       Replace with:
       ```python
       if Stealth is None:
           raise RuntimeError(
               "playwright_stealth is not installed; install it via "
               "fee_crawler/requirements.txt to enable stealth fetching"
           )
       _stealth = Stealth()
       ```

       This preserves runtime behavior when `playwright_stealth` is installed (the Modal image pulls it from `fee_crawler/requirements.txt`), gives a clear error if it is not, and exposes `Stealth` as a real module attribute that `unittest.mock.patch("fee_crawler.pipeline.playwright_fetcher.Stealth", ...)` can resolve.

    3. Do NOT alter any other logic in the file (SSRF blocklists, browser singleton, fetch flow). Do NOT lazy-import any other symbols.
  </action>
  <verify>
    <automated>python -c "from fee_crawler.pipeline import playwright_fetcher; assert hasattr(playwright_fetcher, 'Stealth'), 'Stealth not at module scope'; print('OK')" && pytest fee_crawler/tests/test_stealth_fetcher.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from fee_crawler.pipeline import playwright_fetcher; assert hasattr(playwright_fetcher, 'Stealth')"` exits 0
    - `pytest fee_crawler/tests/test_stealth_fetcher.py -q` exits 0
    - `grep -c "from playwright_stealth import Stealth" fee_crawler/pipeline/playwright_fetcher.py` == 1 (only the hoisted module-scope import remains)
    - The runtime fallback message string is present: `grep -q "playwright_stealth is not installed" fee_crawler/pipeline/playwright_fetcher.py`
  </acceptance_criteria>
  <done>Stealth is at module scope, the test patch target resolves, the test passes.</done>
</task>

</tasks>

<verification>
- pytest fee_crawler/tests/test_stealth_fetcher.py -q passes
- pytest -m "not e2e and not llm and not slow" -q does not regress
- Module imports cleanly even if playwright_stealth is missing (fallback path)
</verification>

<success_criteria>
test_stealth_fetcher.py is no longer red. The Phase 59 regression is closed.
</success_criteria>

<output>
Create `.planning/phases/60.1-audit-remediation-operational-reliability/60.1-06-SUMMARY.md`
</output>
