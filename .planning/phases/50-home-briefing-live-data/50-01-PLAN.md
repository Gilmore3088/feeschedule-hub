---
phase: 50-home-briefing-live-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hamilton/home/WhatChangedCard.tsx
  - src/components/hamilton/home/PriorityAlertsCard.tsx
  - src/components/hamilton/home/MonitorFeedPreview.tsx
  - src/components/hamilton/home/PositioningEvidence.tsx
autonomous: true
requirements: [HOME-02, HOME-03, HOME-05]

must_haves:
  truths:
    - "WhatChangedCard shows onboarding guidance when signals array is empty"
    - "PriorityAlertsCard shows onboarding guidance when alerts array is empty"
    - "MonitorFeedPreview shows onboarding guidance when signals array is empty"
    - "PositioningEvidence shows proper empty state when entries array is empty"
    - "No hardcoded fee amounts, fake signal entries, or CFPB content exist in any of the 4 components"
  artifacts:
    - path: "src/components/hamilton/home/WhatChangedCard.tsx"
      provides: "Signal cards from props only, onboarding empty state"
      contains: "Configure your watchlist to see fee changes here"
    - path: "src/components/hamilton/home/PriorityAlertsCard.tsx"
      provides: "Alert cards from props only, onboarding empty state"
      contains: "No active alerts. Hamilton will flag high-priority changes."
    - path: "src/components/hamilton/home/MonitorFeedPreview.tsx"
      provides: "Signal feed from props only, onboarding empty state"
      contains: "Add institutions to your watchlist to see the signal feed"
    - path: "src/components/hamilton/home/PositioningEvidence.tsx"
      provides: "Positioning from entries prop only, no DefaultStats"
  key_links:
    - from: "WhatChangedCard.tsx"
      to: "page.tsx"
      via: "signals prop"
      pattern: "signals={signals.whatChanged}"
    - from: "PriorityAlertsCard.tsx"
      to: "page.tsx"
      via: "alerts prop"
      pattern: "alerts={signals.priorityAlerts}"
---

<objective>
Strip all hardcoded fallback content from WhatChangedCard, PriorityAlertsCard, MonitorFeedPreview, and PositioningEvidence. Replace empty-data branches with onboarding-guidance empty states styled within the Hamilton design system. After this plan, these 4 components render exclusively from props — no internal default data.

Purpose: Satisfy HOME-02, HOME-03, HOME-05 — every number displayed traces to a real DB query
Output: 4 cleaned components with proper empty states
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-home-briefing-live-data/50-CONTEXT.md
@.planning/phases/50-home-briefing-live-data/50-RESEARCH.md
@src/lib/hamilton/home-data.ts

<interfaces>
<!-- Key types the executor needs -->

From src/lib/hamilton/home-data.ts:
```typescript
export interface SignalEntry {
  id: string;
  signalType: string;
  severity: string;
  title: string;
  body: string;
  createdAt: string;
}

export interface AlertEntry {
  id: string;
  signalId: string;
  severity: string;
  title: string;
  body: string;
  status: string;
  createdAt: string;
}

export interface PositioningEntry {
  feeCategory: string;
  displayName: string;
  medianAmount: number | null;
  p25Amount: number | null;
  p75Amount: number | null;
  institutionCount: number;
  maturityTier: "strong" | "provisional" | "insufficient";
}
```

Hamilton design system tokens (used in all components):
- `var(--hamilton-surface-container-lowest)` — card backgrounds
- `var(--hamilton-on-surface-variant)` — muted text, empty state text color
- `var(--hamilton-text-tertiary)` — secondary helper text
- `var(--hamilton-primary)` — brand color (terracotta)
- `var(--hamilton-error)` — alert/risk indicators
- `var(--hamilton-radius-lg)` — border radius

Empty state pattern (from existing components):
- Paragraph at `0.875rem` with `lineHeight: 1.6`
- Optional secondary line at `0.8125rem` in `var(--hamilton-text-tertiary)`
- Padding: `1rem 0` to `2rem 0`
- Text color: `var(--hamilton-on-surface-variant)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip hardcoded defaults from WhatChangedCard, PriorityAlertsCard, and MonitorFeedPreview</name>
  <files>
    src/components/hamilton/home/WhatChangedCard.tsx
    src/components/hamilton/home/PriorityAlertsCard.tsx
    src/components/hamilton/home/MonitorFeedPreview.tsx
  </files>
  <read_first>
    src/components/hamilton/home/WhatChangedCard.tsx
    src/components/hamilton/home/PriorityAlertsCard.tsx
    src/components/hamilton/home/MonitorFeedPreview.tsx
  </read_first>
  <action>
**WhatChangedCard.tsx** (per D-06, D-08, D-09):
1. Delete the `DEFAULT_CARDS` array (lines ~21-40) and the `DefaultCard` interface entirely
2. In the `signals.length === 0` branch (lines ~60-93), replace the DEFAULT_CARDS rendering with an onboarding empty state:
   - Text: "Configure your watchlist to see fee changes here" (verbatim from D-09)
   - Style: `color: "var(--hamilton-on-surface-variant)"`, `fontSize: "0.875rem"`, `lineHeight: 1.6`, `padding: "2rem 0"`, `textAlign: "center"`
3. Keep the `signals.length > 0` branch unchanged — it renders from props correctly
4. Verify no other hardcoded signal text remains in the file

**PriorityAlertsCard.tsx** (per D-05, D-08, D-09):
1. Delete the `DefaultAlerts` component (lines ~59-113) entirely — both the function and all its hardcoded content ("Overdraft fee is $4 above median", "Complaint language worsening")
2. In the `alerts.length === 0` branch (line ~161), replace `<DefaultAlerts />` with an onboarding empty state:
   - Text: "No active alerts. Hamilton will flag high-priority changes." (verbatim from D-09)
   - Same empty state styling as WhatChangedCard above
3. For the `WhyItMatters` sub-component (lines ~36-55) with 3 hardcoded bullets ("Retention risk is rising", "Peer pricing direction shifting downward", "Revenue exposure is increasing"): Remove this component entirely. It contains fabricated claims that violate HOME-05. The real-data alert items provide their own context.
4. Remove any import or render of `WhyItMatters` from the main component body

**MonitorFeedPreview.tsx** (per D-07, D-08, D-09):
1. Delete the `DEFAULT_FEED` array (lines ~22-35) and the `DefaultFeedItem` interface entirely
2. In the `!hasSignals` branch (lines ~99-171), replace the DEFAULT_FEED rendering with an onboarding empty state:
   - Text: "Add institutions to your watchlist to see the signal feed" (verbatim from D-09)
   - Same empty state styling as WhatChangedCard above
3. Keep the `hasSignals` branch unchanged — it renders from props correctly
4. Verify the hardcoded "CFPB release confirms" and "Heritage First" strings are fully removed
  </action>
  <verify>
    <automated>grep -rn "DEFAULT_CARDS\|DefaultCard\|DefaultAlerts\|DEFAULT_FEED\|DefaultFeedItem\|WhyItMatters\|Overdraft fee is\|Peer median decreased\|Heritage First\|CFPB release confirms\|Retention risk\|Peer pricing direction\|Revenue exposure" src/components/hamilton/home/WhatChangedCard.tsx src/components/hamilton/home/PriorityAlertsCard.tsx src/components/hamilton/home/MonitorFeedPreview.tsx; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "DEFAULT_CARDS" in WhatChangedCard.tsx returns zero matches
    - grep for "DefaultAlerts" in PriorityAlertsCard.tsx returns zero matches
    - grep for "DEFAULT_FEED" in MonitorFeedPreview.tsx returns zero matches
    - grep for "WhyItMatters" in PriorityAlertsCard.tsx returns zero matches
    - grep for "Configure your watchlist" in WhatChangedCard.tsx returns 1+ matches
    - grep for "No active alerts" in PriorityAlertsCard.tsx returns 1+ matches
    - grep for "Add institutions to your watchlist" in MonitorFeedPreview.tsx returns 1+ matches
  </acceptance_criteria>
  <done>All three components render exclusively from props. Empty arrays produce onboarding guidance text per D-09. Zero hardcoded fee amounts, fake signal entries, or CFPB content remain.</done>
</task>

<task type="auto">
  <name>Task 2: Strip hardcoded defaults from PositioningEvidence and fix semantic errors</name>
  <files>
    src/components/hamilton/home/PositioningEvidence.tsx
  </files>
  <read_first>
    src/components/hamilton/home/PositioningEvidence.tsx
  </read_first>
  <action>
**PositioningEvidence.tsx** (per D-08, D-11, HOME-02):

1. Delete the entire `DefaultStats` component (lines ~22-78) — it contains hardcoded `$33.00`, `$29.00`, `88th` values

2. In the `entries.length === 0` branch, replace `<DefaultStats />` with an onboarding empty state using the existing `EmptyState` pattern already in this file (there is an EmptyState component or pattern at line ~14):
   - Text: "Configure your institution in Settings to see positioning data"
   - Style: same Hamilton design system empty state pattern (color: `var(--hamilton-on-surface-variant)`, fontSize `0.875rem`, lineHeight 1.6, padding `2rem 0`, textAlign center)

3. Fix semantic error in the real-data branch:
   - Find where `peerMedian` is assigned from `first?.p25Amount` — this is wrong. P25 is the 25th percentile, NOT the peer median
   - Change `peerMedian` to use `first?.medianAmount` (the actual median value)
   - If `yourFee` also maps to `first?.medianAmount`, rename the label from "Your Fee" to "Market Median" since there is no institution-specific fee in the data layer

4. Remove hardcoded percentile references:
   - Delete the hardcoded `88th` percentile stat (lines ~157-159)
   - Delete the hardcoded progress bar `width: "88%"` (lines ~191-193)
   - Since `PositioningEntry` has no `percentile` field and computing it requires institution-specific fee data not in scope, suppress/hide the Percentile column entirely. Use a conditional check: if no percentile data exists, do not render that column. Replace with a "Coverage" or "Maturity" indicator using the existing `maturityTier` field from `PositioningEntry`.

5. Remove hardcoded labels:
   - "High Outlier" (line ~126) — remove or derive from data comparison (e.g., if medianAmount > p75Amount, label as "Above P75")
   - "Market Benchmark" (line ~144) — remove or replace with `maturityTier` badge
   - "Top Quartile Pricing (High Risk)" (line ~152-153) — remove entirely

6. The component must render its table/grid exclusively from the `entries` prop with real `medianAmount`, `p25Amount`, `p75Amount`, `institutionCount`, and `maturityTier` values
  </action>
  <verify>
    <automated>grep -rn "DefaultStats\|\\\$33\|\\\$29\|88th\|88%\|High Outlier\|Top Quartile Pricing" src/components/hamilton/home/PositioningEvidence.tsx; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "DefaultStats" in PositioningEvidence.tsx returns zero matches
    - grep for "88th" in PositioningEvidence.tsx returns zero matches
    - grep for "88%" in PositioningEvidence.tsx returns zero matches
    - grep for "$33" in PositioningEvidence.tsx returns zero matches
    - grep for "p25Amount" used as peerMedian is gone — grep for "p25Amount" near "peerMedian" returns zero matches
    - grep for "Configure your institution" OR "positioning data" in PositioningEvidence.tsx returns 1+ matches
    - grep for "medianAmount" in PositioningEvidence.tsx returns 1+ matches (real data used)
  </acceptance_criteria>
  <done>PositioningEvidence renders exclusively from `entries` prop. No hardcoded dollar amounts, percentiles, or labels remain. peerMedian correctly uses medianAmount. Empty state shows onboarding guidance. Percentile column suppressed until institution-specific data is available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server data -> Component props | Data fetched server-side, no user input in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Information Disclosure | PositioningEvidence | accept | Components display aggregate national index data (public); no PII or institution-specific data exposed |
| T-50-02 | Spoofing | Empty states | accept | Empty states are static text strings with no user input; no injection vector |
</threat_model>

<verification>
Run the combined grep audit to confirm all hardcoded content is removed from the 4 component files:

```bash
grep -rn "DEFAULT_CARDS\|DefaultCard\|DefaultAlerts\|DEFAULT_FEED\|DefaultFeedItem\|DefaultStats\|WhyItMatters\|\\\$33\|\\\$29\|88th\|88%\|Overdraft fee is\|Peer median decreased\|Heritage First\|CFPB release confirms\|Retention risk\|High Outlier\|Top Quartile Pricing" \
  src/components/hamilton/home/WhatChangedCard.tsx \
  src/components/hamilton/home/PriorityAlertsCard.tsx \
  src/components/hamilton/home/MonitorFeedPreview.tsx \
  src/components/hamilton/home/PositioningEvidence.tsx
```

Expected: zero matches.

Confirm onboarding text exists:
```bash
grep -rn "Configure your watchlist\|No active alerts\|Add institutions to your watchlist\|Configure your institution" \
  src/components/hamilton/home/
```

Expected: 4 matches (one per component).
</verification>

<success_criteria>
- All 4 components render exclusively from props
- Empty arrays produce styled onboarding guidance (D-09)
- Zero hardcoded fee amounts ($33, $29), percentages (88th, 88%), fake signals, or CFPB content
- PositioningEvidence uses medianAmount correctly (not p25Amount for peer median)
- TypeScript compiles without errors: `npx tsc --noEmit`
</success_criteria>

<output>
After completion, create `.planning/phases/50-home-briefing-live-data/50-01-SUMMARY.md`
</output>
