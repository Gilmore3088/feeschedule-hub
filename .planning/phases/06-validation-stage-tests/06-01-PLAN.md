---
phase: 06-validation-stage-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fee_crawler/tests/e2e/test_validation_stage.py
autonomous: true
requirements:
  - VALD-01
  - VALD-02
must_haves:
  truths:
    - "fees with extraction_confidence >= 0.85 and no blocking flags transition review_status to 'staged'"
    - "fees with extraction_confidence < 0.85 and no blocking flags remain 'pending'"
    - "fees with blocking flags (error/warning) transition to 'flagged' regardless of confidence"
    - "statistical outliers (IQR-based) are flagged after run_outlier_detection()"
    - "validation_flags is valid JSON for all rows where it is non-null"
  artifacts:
    - path: "fee_crawler/tests/e2e/test_validation_stage.py"
      provides: "VALD-01 and VALD-02 test coverage"
      exports:
        - "test_confidence_threshold_transitions"
        - "test_outlier_detection_flags_statistical_outlier"
  key_links:
    - from: "test_validation_stage.py (validated_db fixture)"
      to: "fee_crawler.commands.backfill_validation.run()"
      via: "direct Python import, no subprocess"
      pattern: "from fee_crawler.commands.backfill_validation import run as validate_run"
    - from: "test_validation_stage.py (outlier_db fixture)"
      to: "fee_crawler.pipeline.outlier_detection.run_outlier_detection()"
      via: "direct Python import, no subprocess"
      pattern: "from fee_crawler.pipeline.outlier_detection import run_outlier_detection"
---

<objective>
Create the validation stage test file with two function-scoped fixtures and two tests
covering VALD-01 (confidence-threshold review_status transitions) and VALD-02 (statistical
outlier detection via IQR). Uses synthetic pre-populated extracted_fees rows with known
confidence values and amounts. No LLM calls, no network.

Purpose: Prove that backfill_validation.run() correctly transitions review_status based on
confidence thresholds, and that run_outlier_detection() flags fees whose amounts are
statistically improbable (IQR-based, 5+ peer minimum).

Output: fee_crawler/tests/e2e/test_validation_stage.py (2 fixtures, 2 tests)
</objective>

<context>
@.planning/phases/06-validation-stage-tests/06-CONTEXT.md
@fee_crawler/tests/e2e/conftest.py
@fee_crawler/tests/e2e/test_categorization_stage.py
@fee_crawler/commands/backfill_validation.py
@fee_crawler/pipeline/outlier_detection.py
@fee_crawler/validation.py
@fee_crawler/config.py
@fee_crawler/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write validated_db fixture and VALD-01 confidence threshold test</name>
  <files>fee_crawler/tests/e2e/test_validation_stage.py</files>
  <action>
Create fee_crawler/tests/e2e/test_validation_stage.py with module docstring, imports,
a function-scoped `validated_db` fixture, and the VALD-01 test.

The fixture must:
1. Clean slate - DELETE in FK order: extracted_fees, crawl_results, crawl_runs (trigger='test'), crawl_targets
2. Insert FK scaffold: one crawl_targets, one crawl_runs, one crawl_results row
3. Insert synthetic extracted_fees rows with known confidence values:
   - fee_name="High Confidence Fee", fee_category="monthly_maintenance",
     amount=12.0, extraction_confidence=0.90 (expect: staged)
   - fee_name="Exact Threshold Fee", fee_category="monthly_maintenance",
     amount=10.0, extraction_confidence=0.85 (expect: staged - boundary inclusive)
   - fee_name="Below Threshold Fee", fee_category="overdraft",
     amount=25.0, extraction_confidence=0.84 (expect: pending - just below threshold)
   - fee_name="Low Confidence Fee", fee_category="nsf",
     amount=30.0, extraction_confidence=0.70 (expect: pending)
   - fee_name="Blocking Flag Fee", fee_category="monthly_maintenance",
     amount=99999.0, extraction_confidence=0.90 (expect: flagged - amount exceeds hard ceiling)
4. Run backfill_validation.run(test_db, test_config)
5. Yield test_db for test use
6. Teardown: DELETE in FK order

The VALD-01 test `test_confidence_threshold_transitions(validated_db)`:
- Fetch all rows from extracted_fees
- Build a dict keyed by fee_name for easy lookup
- Assert High Confidence Fee has review_status='staged'
- Assert Exact Threshold Fee has review_status='staged' (boundary: 0.85 IS staged)
- Assert Below Threshold Fee has review_status='pending'
- Assert Low Confidence Fee has review_status='pending'
- Assert Blocking Flag Fee has review_status='flagged'
- For all rows where validation_flags IS NOT NULL, parse JSON and assert it's a list
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && python -m pytest fee_crawler/tests/e2e/test_validation_stage.py::test_confidence_threshold_transitions -v --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>test_confidence_threshold_transitions collects and PASSES. validated_db fixture inserts, validates, and tears down cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Write outlier_db fixture and VALD-02 outlier detection test</name>
  <files>fee_crawler/tests/e2e/test_validation_stage.py</files>
  <action>
Add a function-scoped `outlier_db` fixture and the VALD-02 test to the existing file.

The outlier_db fixture must:
1. Clean slate - DELETE in FK order
2. Insert FK scaffold (same pattern as validated_db)
3. Insert 6 synthetic extracted_fees rows for category "atm_non_network":
   - 5 "normal" fees with amounts [2.0, 3.0, 3.5, 4.0, 5.0], confidence=0.90,
     review_status='staged' directly (INSERT with review_status='staged')
   - 1 outlier fee with amount=500.0 (extreme - far above IQR upper bound),
     confidence=0.90, review_status='staged'
   NOTE: run_outlier_detection() requires >= 5 rows in staged/approved to compute stats.
   The 5 normal rows are in staged status with amounts clustered around 3-5 dollars.
   The outlier at $500 is statistically extreme.
4. Call run_outlier_detection(test_db) to flag outliers
5. Yield test_db for test use
6. Teardown: DELETE in FK order

The VALD-02 test `test_outlier_detection_flags_statistical_outlier(outlier_db)`:
- Query extracted_fees for the fee with amount=500.0
- Assert its review_status == 'flagged'
- Assert its validation_flags IS NOT NULL
- Parse validation_flags as JSON and assert it contains at least one flag entry
- Assert each flag entry has 'rule', 'severity', and 'message' keys
- Query the 5 normal fees (amount < 10.0) and assert their review_status is still 'staged'
  (normal fees must NOT be flagged by outlier detection)
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && python -m pytest fee_crawler/tests/e2e/test_validation_stage.py -v --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>Both tests collect and PASS. VALD-01 verified (confidence thresholds). VALD-02 verified (outlier flagging). Test file under 200 lines.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Import check: python -c "import fee_crawler.tests.e2e.test_validation_stage" exits 0
2. Full test run: python -m pytest fee_crawler/tests/e2e/test_validation_stage.py -v -- both PASSED
3. Marker filter: python -m pytest -m e2e fee_crawler/tests/e2e/test_validation_stage.py --collect-only shows 2 tests
4. Existing tests unbroken: python -m pytest fee_crawler/tests/e2e/test_categorization_stage.py -v still passes
</verification>

<success_criteria>
- fee_crawler/tests/e2e/test_validation_stage.py exists
- validated_db fixture: function-scoped, 5 synthetic rows with known confidence values, runs backfill_validation, tears down cleanly
- outlier_db fixture: function-scoped, 6 rows in atm_non_network (5 normal staged + 1 extreme outlier staged), runs run_outlier_detection, tears down cleanly
- test_confidence_threshold_transitions: PASSED -- VALD-01 verified
- test_outlier_detection_flags_statistical_outlier: PASSED -- VALD-02 verified
- Both tests marked @pytest.mark.e2e only
- data/crawler.db modification time unchanged after test run
</success_criteria>

<output>
After completion, create .planning/phases/06-validation-stage-tests/06-01-SUMMARY.md
following the template at $HOME/.claude/get-shit-done/templates/summary.md.
</output>
