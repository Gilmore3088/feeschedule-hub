---
phase: 29-consumer-landing-page
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - src/app/gateway-client.tsx
autonomous: false
requirements: [CLND-08]

must_haves:
  truths:
    - "gateway-client.tsx is deleted and no other file imports it"
    - "Landing page meets consulting-grade visual quality bar when viewed in browser"
    - "Page renders correctly at mobile (375px), tablet (768px), and desktop (1280px) widths"
  artifacts: []
  key_links: []
---

<objective>
Clean up the replaced gateway component and verify the landing page visually meets the consulting-grade quality bar (CLND-08).

Purpose: Remove dead code and confirm the editorial typography, whitespace, and overall presentation match the Salesforce Connected FINS quality benchmark.
Output: Deleted gateway-client.tsx, visual sign-off on landing page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-consumer-landing-page/29-CONTEXT.md
@.planning/phases/29-consumer-landing-page/29-UI-SPEC.md
@.planning/phases/29-consumer-landing-page/29-01-SUMMARY.md

@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete gateway-client.tsx and verify no broken imports</name>
  <files>src/app/gateway-client.tsx</files>
  <read_first>
    - src/app/page.tsx (confirm it no longer imports gateway-client)
    - src/app/gateway-client.tsx (file to delete)
  </read_first>
  <action>
1. Search the entire `src/` directory for any remaining imports of `gateway-client`:
   ```
   grep -r "gateway-client" src/
   ```
   If any file still imports it, update that file to remove the import.

2. Delete `src/app/gateway-client.tsx`.

3. Run `npx tsc --noEmit` to confirm no broken imports across the project.

4. Run `npx next build` (or at minimum `npx tsc --noEmit`) to confirm the build succeeds without gateway-client.tsx.
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && test ! -f src/app/gateway-client.tsx && echo "DELETED" && grep -r "gateway-client" src/ 2>/dev/null; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/gateway-client.tsx does NOT exist (file deleted)
    - grep -r "gateway-client" src/ returns no matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>gateway-client.tsx deleted with no broken imports anywhere in the codebase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual quality verification of landing page</name>
  <files>src/app/page.tsx</files>
  <what-built>
Complete consumer landing page at `/` with:
- Hero section: Newsreader serif headline "What is your bank really charging you?" with embedded Fee Scout search bar
- 3 value prop cards: Find Your Fees, Compare Banks, Learn About Fees
- Trust stats strip with live data (institution count, observations, freshness, FDIC/NCUA)
- Consumer footer
- No B2B section (professional access via nav link)
  </what-built>
  <action>
Start dev server with `npm run dev`. Open browser to http://localhost:3000/. Verify the 7-point checklist in how-to-verify below. Take screenshots at 375px, 768px, and 1280px widths.
  </action>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Visit `http://localhost:3000/`
3. **Hero section check:**
   - Headline uses serif font (Newsreader), "really" is italicized
   - Institution count in supporting text is a real number (not "8,000")
   - Search bar is visible and functional -- type "chase" and see autocomplete results
   - No login prompt appears at any point
4. **Value prop cards check:**
   - 3 cards in a row on desktop, stacked on mobile
   - Each card has an icon, label, title, body text, and arrow
   - Click "Find Your Fees" card -- navigates to /institutions
   - Click "Compare Banks" card -- navigates to /fees
   - Click "Learn About Fees" card -- navigates to /guides
   - Hover state: border turns terracotta, shadow appears
5. **Trust stats check:**
   - 4 stats in a horizontal row on desktop, 2x2 on mobile
   - Numbers are real (non-zero), formatted with commas
   - "FDIC & NCUA" appears as verified sources
6. **Visual quality (CLND-08):**
   - Generous whitespace between sections
   - Editorial typography feel (not generic SaaS)
   - Warm cream background (#FAF7F2)
   - No clutter, no stock photography patterns
   - Resize browser to mobile width (375px) -- layout adapts cleanly
7. **View page source** -- confirm server-rendered HTML (not empty div with JS loading)
  </how-to-verify>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && curl -s http://localhost:3000/ | grep -c "What is your bank"</automated>
  </verify>
  <acceptance_criteria>
    - curl http://localhost:3000/ returns HTML containing "What is your bank"
    - Page source does NOT contain "GatewayClient"
    - Human confirms visual quality passes CLND-08 bar
  </acceptance_criteria>
  <done>Human has approved the landing page visual quality at all viewport sizes.</done>
  <resume-signal>Type "approved" or describe specific visual issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Cleanup and verification only -- no new boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | I (Information Disclosure) | gateway-client.tsx deletion | accept | File contained no secrets; deletion reduces attack surface by removing unused client code |
</threat_model>

<verification>
1. `test ! -f src/app/gateway-client.tsx` passes (file deleted)
2. `npx tsc --noEmit` exits 0 (no broken imports)
3. Human confirms visual quality at mobile, tablet, and desktop widths
</verification>

<success_criteria>
- gateway-client.tsx is deleted with zero broken references
- Human approves the landing page visual quality meets consulting-grade bar
- Page renders correctly at all viewport sizes
</success_criteria>

<output>
After completion, create `.planning/phases/29-consumer-landing-page/29-02-SUMMARY.md`
</output>
