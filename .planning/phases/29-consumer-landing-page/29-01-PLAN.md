---
phase: 29-consumer-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/landing-hero.tsx
  - src/app/landing-value-cards.tsx
  - src/app/landing-trust-stats.tsx
autonomous: true
requirements: [CLND-01, CLND-02, CLND-03, CLND-04, CLND-05, CLND-06, CLND-07]

must_haves:
  truths:
    - "Visitor at / sees a server-rendered landing page with hero headline, search bar, value cards, and trust stats"
    - "Visitor can type a bank name into the hero search bar and see autocomplete results without logging in"
    - "Trust stats display live numbers from the database, not hardcoded values"
    - "Value prop cards link to /institutions, /fees, and /guides respectively"
    - "No B2B section appears on the page -- B2B access is via nav link only"
    - "Page has correct generateMetadata() with title, description, and OG tags"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Server Component landing page with generateMetadata"
      contains: "generateMetadata"
    - path: "src/app/landing-hero.tsx"
      provides: "Hero section with search bar"
      contains: "InstitutionSearchBar"
    - path: "src/app/landing-value-cards.tsx"
      provides: "3 value prop cards"
      contains: "/institutions"
    - path: "src/app/landing-trust-stats.tsx"
      provides: "Trust stats strip from live data"
      contains: "getPublicStats"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/landing-hero.tsx"
      via: "import LandingHero"
      pattern: "import.*LandingHero"
    - from: "src/app/landing-hero.tsx"
      to: "src/app/(public)/institutions/search-bar.tsx"
      via: "import InstitutionSearchBar"
      pattern: "InstitutionSearchBar"
    - from: "src/app/landing-trust-stats.tsx"
      to: "src/lib/crawler-db/core.ts"
      via: "getPublicStats + getDataFreshness"
      pattern: "getPublicStats|getDataFreshness"
---

<objective>
Build the consumer landing page that replaces the split-panel gateway at `/`. The page has 3 sections: hero with embedded Fee Scout search, 3 value prop cards, and a trust stats strip with live data. This is a Server Component page with only the search bar introducing a client boundary.

Purpose: Convert the root URL from a client-rendered split-panel chooser into a server-rendered, SEO-indexable consumer landing page that convinces visitors to search their bank.
Output: 4 new/modified files forming the complete landing page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-consumer-landing-page/29-CONTEXT.md
@.planning/phases/29-consumer-landing-page/29-UI-SPEC.md

@src/app/page.tsx
@src/app/gateway-client.tsx
@src/app/(public)/institutions/search-bar.tsx
@src/lib/crawler-db/core.ts
@src/app/layout.tsx
@src/components/customer-footer.tsx

<interfaces>
From src/lib/crawler-db/core.ts:
```typescript
export interface PublicStats {
  total_observations: number;
  total_institutions: number;
  total_categories: number;
  total_states: number;
}
export async function getPublicStats(): Promise<PublicStats>;
export async function getDataFreshness(): Promise<DataFreshness>;
// DataFreshness has: last_crawl_at, last_fee_extracted_at, total_observations
```

From src/app/(public)/institutions/search-bar.tsx:
```typescript
export function InstitutionSearchBar({ autoFocus = false }: { autoFocus?: boolean });
// "use client" component with debounced search, click-outside, dropdown results
```

From src/components/customer-footer.tsx:
```typescript
export function CustomerFooter();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hero section and rewrite page.tsx as Server Component landing</name>
  <files>src/app/page.tsx, src/app/landing-hero.tsx</files>
  <read_first>
    - src/app/page.tsx (current gateway page to replace)
    - src/app/gateway-client.tsx (content being replaced -- understand what exists)
    - src/app/(public)/institutions/search-bar.tsx (search bar to embed in hero)
    - src/app/layout.tsx (font variables available: --font-newsreader, --font-geist-sans)
    - .planning/phases/29-consumer-landing-page/29-UI-SPEC.md (visual contract)
    - .planning/phases/29-consumer-landing-page/29-CONTEXT.md (decisions D-01 through D-15)
  </read_first>
  <action>
**Create `src/app/landing-hero.tsx`** — a client component (because it wraps InstitutionSearchBar which is "use client"):

```
"use client";
```

Import `InstitutionSearchBar` from `@/app/(public)/institutions/search-bar`.

Layout: `mx-auto max-w-6xl px-6 py-16 lg:py-20` outer container. Content constrained to `max-w-2xl mx-auto text-center`.

Hero headline as `<h1>` with Newsreader font:
- Style: `font-[var(--font-newsreader)] text-[clamp(28px,5vw,40px)] font-normal leading-[1.08] tracking-[-0.02em] text-[#1A1815]`
- Text: `What is your bank <em class="italic">really</em> charging you?`
- The `<em>` tag uses italic style (Newsreader italic loaded in layout.tsx)

Supporting text below h1:
- `<p>` with `mt-4 text-[14px] leading-relaxed text-[#6B6355]`
- Text template: `Compare fees across {totalInstitutions}+ banks and credit unions. Free, no account required.`
- Receive `totalInstitutions` as a prop (number) from page.tsx

Search bar wrapper:
- `<div className="mt-8 mx-auto max-w-xl">`
- Render `<InstitutionSearchBar />` (no autoFocus, default behavior)
- Add `aria-label="Search for a bank or credit union"` to the wrapping div

Subtle gradient accent (existing consumer pattern):
- Position a `<div>` with `absolute top-0 right-0 w-1/2 h-full pointer-events-none bg-gradient-to-l from-[#C44B2E]/[0.03] to-transparent`
- The hero section wrapper needs `relative overflow-hidden`

Props interface:
```typescript
interface LandingHeroProps {
  totalInstitutions: number;
}
```

No animation on page load per UI-SPEC decision. No "use server" directives in this file.

**Rewrite `src/app/page.tsx`** as a Server Component (no "use client"):

Remove the GatewayClient import entirely.

Add `generateMetadata()` export:
```typescript
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Bank Fee Index -- Find What Your Bank Really Charges",
  description: "Compare fees across 8,000+ banks and credit unions. See how your bank's fees compare to the national median. Free, no account required.",
  openGraph: {
    title: "Bank Fee Index -- Find What Your Bank Really Charges",
    description: "Compare fees across 8,000+ banks and credit unions. See how your bank's fees compare to the national median.",
  },
};
```

In the default export function (async Server Component):
1. Import and call `getPublicStats()` from `@/lib/crawler-db/core`
2. Import and call `getDataFreshness()` from `@/lib/crawler-db/core`
3. Import `LandingHero` from `./landing-hero`
4. Import `LandingValueCards` from `./landing-value-cards`
5. Import `LandingTrustStats` from `./landing-trust-stats`
6. Import `CustomerFooter` from `@/components/customer-footer`

Page wrapper: `<div className="min-h-screen bg-[#FAF7F2] consumer-brand">`

Render in order:
- `<LandingHero totalInstitutions={stats.total_institutions} />`
- `<LandingValueCards />`
- `<LandingTrustStats stats={stats} freshness={freshness} />`
- `<CustomerFooter />`

Do NOT delete gateway-client.tsx yet (it may be imported elsewhere). Just remove its import from page.tsx.

Per D-10, D-11, D-12: No B2B section on the page. CLND-07 is satisfied by ConsumerNav's "Professional" link (from Phase 28) which will appear in the layout.
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && npx tsc --noEmit src/app/page.tsx src/app/landing-hero.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx does NOT contain "use client"
    - src/app/page.tsx does NOT contain "GatewayClient" or "gateway-client"
    - src/app/page.tsx contains "generateMetadata" or "export const metadata"
    - src/app/page.tsx contains "getPublicStats"
    - src/app/page.tsx contains "getDataFreshness"
    - src/app/page.tsx contains "LandingHero"
    - src/app/page.tsx contains "LandingValueCards"
    - src/app/page.tsx contains "LandingTrustStats"
    - src/app/page.tsx contains "CustomerFooter"
    - src/app/page.tsx contains "consumer-brand"
    - src/app/landing-hero.tsx contains "use client"
    - src/app/landing-hero.tsx contains "InstitutionSearchBar"
    - src/app/landing-hero.tsx contains "What is your bank"
    - src/app/landing-hero.tsx contains "really"
    - src/app/landing-hero.tsx contains "font-newsreader" or "var(--font-newsreader)"
    - src/app/landing-hero.tsx contains "totalInstitutions"
    - src/app/landing-hero.tsx contains "clamp(28px"
    - src/app/landing-hero.tsx does NOT contain "hardcoded" or "8,000" or "8000" (uses dynamic prop)
  </acceptance_criteria>
  <done>page.tsx is a Server Component with generateMetadata, fetches live stats, renders hero with embedded search bar. Hero displays Newsreader headline "What is your bank really charging you?" with dynamic institution count. No auth gate on search.</done>
</task>

<task type="auto">
  <name>Task 2: Create value prop cards and trust stats sections</name>
  <files>src/app/landing-value-cards.tsx, src/app/landing-trust-stats.tsx</files>
  <read_first>
    - src/app/page.tsx (just created -- see props passed to these components)
    - .planning/phases/29-consumer-landing-page/29-UI-SPEC.md (Section 2 and Section 3 specs)
    - .planning/phases/29-consumer-landing-page/29-CONTEXT.md (D-04, D-06, D-09 decisions)
    - src/lib/crawler-db/core.ts (PublicStats and DataFreshness interfaces)
  </read_first>
  <action>
**Create `src/app/landing-value-cards.tsx`** — Server Component (no "use client"):

Import from lucide-react: `Search`, `BarChart2`, `BookOpen`, `ArrowRight`.
Import `Link` from `next/link`.

Outer wrapper: `<section className="mx-auto max-w-6xl px-6 pb-16 lg:pb-20">`.

Grid: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">`.

3 cards, each as a `<Link>` with `group` class:

**Card 1 — Find Your Fees:**
- href: `/institutions`
- Icon: `<Search className="h-4 w-4 text-[#C44B2E]" />`
- Label: `<span className="text-[12px] font-normal uppercase tracking-[0.1em] text-[#A09788]">Find Your Fees</span>`
- Title: `<h3 className="mt-2 text-[14px] font-normal uppercase tracking-[0.05em] text-[#1A1815]">Look Up Your Bank</h3>`
- Body: `<p className="mt-2 text-[14px] leading-relaxed text-[#6B6355]">Look up any bank or credit union and see every fee we track, compared to the national median.</p>`
- Arrow: `<ArrowRight className="h-3.5 w-3.5 text-[#A09788] group-hover:text-[#C44B2E] transition-colors" />`

**Card 2 — Compare Banks:**
- href: `/fees`
- Icon: `<BarChart2 className="h-4 w-4 text-[#C44B2E]" />`
- Label: `Compare Banks`
- Title: `Browse Fee Data`
- Body: `Browse fee data across categories -- from overdraft to wire transfers -- with national benchmarks.`

**Card 3 — Learn About Fees (per D-06, consumer guide teaser):**
- href: `/guides`
- Icon: `<BookOpen className="h-4 w-4 text-[#C44B2E]" />`
- Label: `Learn About Fees`
- Title: `Fee Guides`
- Body: `Plain-language guides explaining what each fee means, why banks charge it, and how to avoid it.`

Card container classes (all 3 identical):
```
rounded-xl border border-[#E8DFD1] bg-white p-6 hover:border-[#C44B2E]/30 hover:shadow-md transition-all duration-300
```

Card inner layout: flex flex-col. Icon at top, label below icon, title below label, body below title, arrow at bottom right via `mt-auto pt-4 self-end`.

**Create `src/app/landing-trust-stats.tsx`** — Server Component (no "use client"):

Import `timeAgo` from `@/lib/format` for formatting the freshness date.

Props interface:
```typescript
import type { PublicStats } from "@/lib/crawler-db/core";

interface LandingTrustStatsProps {
  stats: PublicStats;
  freshness: { last_crawl_at: string | null; last_fee_extracted_at: string | null; total_observations: number };
}
```

Outer wrapper: `<section className="border-t border-[#E8DFD1] bg-[#F5EFE6]/60">`.
Inner: `<div className="mx-auto max-w-6xl px-6 py-10">`.

Use a `<dl>` element for semantic structure (per accessibility spec).

Layout: `grid grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8`.

4 stat items, each as a `<div>` containing `<dd>` (value) and `<dt>` (label):

1. Value: `{stats.total_institutions.toLocaleString()}+` | Label: `Banks & Credit Unions`
2. Value: `{stats.total_observations.toLocaleString()}+` | Label: `Fee Observations`
3. Value: `Updated {timeAgo(freshness.last_crawl_at)}` | Label: `Data Freshness` (if last_crawl_at is null, show "Recently")
4. Value: `FDIC & NCUA` | Label: `Verified Sources`

Stat value style: `text-[28px] font-bold text-[#1A1815] tabular-nums` (Geist Sans inherited).
Stat label style: `text-[12px] font-normal text-[#7A7062] uppercase tracking-wide mt-1`.

For stat 3 (Data Freshness), the value text should be `text-[28px] font-bold text-[#1A1815]` but the "Updated" prefix can be `text-[14px] font-normal text-[#6B6355]` with the date portion bold, OR the entire value at 28px/700 -- use your judgment for what reads best at that size. If "Updated 2 days ago" is too long at 28px, use `text-[20px]` for this stat only.

All numbers from live data per D-09. Zero hardcoded numbers.
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && npx tsc --noEmit src/app/landing-value-cards.tsx src/app/landing-trust-stats.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/landing-value-cards.tsx does NOT contain "use client"
    - src/app/landing-value-cards.tsx contains "/institutions"
    - src/app/landing-value-cards.tsx contains "/fees"
    - src/app/landing-value-cards.tsx contains "/guides"
    - src/app/landing-value-cards.tsx contains "Find Your Fees"
    - src/app/landing-value-cards.tsx contains "Compare Banks"
    - src/app/landing-value-cards.tsx contains "Learn About Fees"
    - src/app/landing-value-cards.tsx contains "ArrowRight"
    - src/app/landing-value-cards.tsx contains "group"
    - src/app/landing-value-cards.tsx contains "hover:border-[#C44B2E]"
    - src/app/landing-trust-stats.tsx does NOT contain "use client"
    - src/app/landing-trust-stats.tsx contains "getPublicStats" or "PublicStats" or "total_institutions"
    - src/app/landing-trust-stats.tsx contains "toLocaleString"
    - src/app/landing-trust-stats.tsx contains "FDIC"
    - src/app/landing-trust-stats.tsx contains "NCUA"
    - src/app/landing-trust-stats.tsx contains "<dl" or "dl"
    - src/app/landing-trust-stats.tsx contains "tabular-nums"
    - src/app/landing-trust-stats.tsx does NOT contain any hardcoded institution count like "8000" or "8,000"
  </acceptance_criteria>
  <done>Value prop cards render 3 linked cards (Find/Compare/Learn) with correct hrefs, hover states, and editorial typography. Trust stats strip renders 4 live-data stats in a semantic dl element with proper number formatting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> search API | User input in search bar hits /api/institutions endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | I (Information Disclosure) | getPublicStats / getDataFreshness | accept | Only aggregate counts exposed (institution count, observation count); no PII, no per-institution detail |
| T-29-02 | S (Spoofing) | InstitutionSearchBar API call | accept | Search endpoint is intentionally public and unauthenticated per CLND-03; no write operations |
| T-29-03 | D (Denial of Service) | /api/institutions search | mitigate | Existing checkPublicRateLimit() already applied to /api/v1/* endpoints; search endpoint uses same rate limiter |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no errors on all 4 files
2. `curl -s http://localhost:3000/ | grep -c "What is your bank"` returns 1 (server-rendered)
3. `curl -s http://localhost:3000/ | grep -c "GatewayClient"` returns 0
4. Search bar appears in page and accepts input without login prompt
5. Trust stats show non-zero dynamic numbers
</verification>

<success_criteria>
- Root URL / renders a server-side HTML page with hero, cards, and trust stats
- No "use client" on page.tsx -- only landing-hero.tsx has client boundary
- generateMetadata produces correct title and description
- InstitutionSearchBar works without authentication
- All 3 value prop cards link to correct destinations
- Trust stats display live data from getPublicStats and getDataFreshness
- gateway-client.tsx is no longer imported by page.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/29-consumer-landing-page/29-01-SUMMARY.md`
</output>
