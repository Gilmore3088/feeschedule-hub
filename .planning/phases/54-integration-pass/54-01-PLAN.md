---
phase: 54-integration-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hamilton/home/HamiltonViewCard.tsx
autonomous: false
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04

must_haves:
  truths:
    - "Simulate Change button on Home navigates to /pro/simulate"
    - "Generate Board Brief button on Home navigates to /pro/reports"
    - "Ask Hamilton button on Home navigates to /pro/analyze"
    - "Simulate -> Reports passes scenario_id in URL and Reports page reads it"
    - "Analyze -> Export PDF produces a downloadable PDF blob"
    - "Institution name, type, asset tier, fed_district are identical across all Hamilton screens"
  artifacts:
    - path: "src/components/hamilton/home/HamiltonViewCard.tsx"
      provides: "Three CTA buttons wired to correct routes"
      contains: "/pro/simulate"
  key_links:
    - from: "src/components/hamilton/home/HamiltonViewCard.tsx"
      to: "/pro/simulate, /pro/reports, /pro/analyze"
      via: "next/link href"
      pattern: "href=./pro/(simulate|reports|analyze)"
    - from: "src/components/hamilton/simulate/SimulateWorkspace.tsx"
      to: "/pro/reports?scenario_id="
      via: "router.push"
      pattern: "scenario_id="
    - from: "src/components/hamilton/analyze/AnalyzeWorkspace.tsx"
      to: "/api/pro/report-pdf"
      via: "fetch POST"
      pattern: "report-pdf"
---

<objective>
Wire the 3 static action buttons on HamiltonViewCard to their target screens, then verify all cross-screen integration flows work end-to-end: Home CTAs, Simulate -> Reports scenario handoff, Analyze -> PDF export, and Settings -> institutional context propagation.

Purpose: Phase 54 is the final integration pass for v8.1. Every CTA and data handoff must work correctly so a Pro user can navigate the full Hamilton experience without dead-end buttons or lost context.

Output: HamiltonViewCard buttons wired; all 4 integration flows verified working.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-integration-pass/54-CONTEXT.md

@src/components/hamilton/home/HamiltonViewCard.tsx
@src/components/hamilton/home/RecommendedActionCard.tsx
@src/components/hamilton/simulate/SimulateWorkspace.tsx
@src/components/hamilton/analyze/AnalyzeWorkspace.tsx
@src/components/hamilton/analyze/AnalyzeCTABar.tsx
@src/app/pro/(hamilton)/reports/page.tsx
@src/app/pro/(hamilton)/layout.tsx
@src/lib/auth.ts

<interfaces>
From src/components/hamilton/home/HamiltonViewCard.tsx (server component, NO "use client"):
- 3 static `<button>` elements at lines 267-314: "Simulate Change", "Generate Board Brief", "Ask Hamilton"
- No onClick handlers, no Link imports — these are dead buttons currently

From src/components/hamilton/simulate/SimulateWorkspace.tsx:
- handleGenerateSummary navigates via `router.push(/pro/reports?scenario_id=${scenarioId})`
- Already wired and functional

From src/app/pro/(hamilton)/reports/page.tsx:
- Reads `scenario_id` from Promise-based searchParams
- Passes `initialScenarioId` to ReportWorkspace

From src/components/hamilton/analyze/AnalyzeWorkspace.tsx:
- handleExportPdf fetches POST /api/pro/report-pdf
- Triggers blob download

From src/app/pro/(hamilton)/layout.tsx:
- getCurrentUser() returns institution_name, institution_type, asset_tier, fed_district
- Builds institutionContext object passed to HamiltonShell
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire HamiltonViewCard action buttons to target routes</name>
  <files>src/components/hamilton/home/HamiltonViewCard.tsx</files>
  <read_first>
    - src/components/hamilton/home/HamiltonViewCard.tsx (full file — understand current structure)
    - src/components/hamilton/home/RecommendedActionCard.tsx (reference for how it links to /pro/simulate)
  </read_first>
  <action>
HamiltonViewCard is a server component (no "use client" directive). The 3 action buttons at lines 267-314 are plain `<button>` elements with no onClick handlers.

Replace the 3 `<button>` elements with `Link` components from `next/link`. This keeps the component as a server component (no "use client" needed). Add `import Link from "next/link"` at the top.

Per D-02: "Simulate Change" button -> href="/pro/simulate"
Per D-03: "Generate Board Brief" button -> href="/pro/reports"
Per D-04: "Ask Hamilton" button -> href="/pro/analyze"

Each Link must preserve the exact existing inline styles and className values. The only change is:
1. `<button ...>` becomes `<Link href="/pro/simulate" ...>` (etc.)
2. Remove `cursor: "pointer"` from inline styles (links have pointer by default)
3. Remove `border: "none"` where present (links don't have borders)
4. Add `textDecoration: "none"` to each Link's style object to prevent underline

Do NOT add "use client". Do NOT use useRouter. Link works in server components.
Per D-05: Do NOT wire "Full Dashboard" or "Export PDF" header buttons — those are deferred to v8.2.
  </action>
  <verify>
    <automated>grep -n 'href="/pro/simulate"\|href="/pro/reports"\|href="/pro/analyze"' src/components/hamilton/home/HamiltonViewCard.tsx | wc -l | grep -q 3 && echo "PASS: 3 links found" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `href="/pro/simulate"` in HamiltonViewCard.tsx returns 1 match
    - grep for `href="/pro/reports"` in HamiltonViewCard.tsx returns 1 match
    - grep for `href="/pro/analyze"` in HamiltonViewCard.tsx returns 1 match
    - grep for `"use client"` in HamiltonViewCard.tsx returns 0 matches (stays server component)
    - grep for `import Link` in HamiltonViewCard.tsx returns 1 match
  </acceptance_criteria>
  <done>All 3 HamiltonViewCard action buttons are Link elements navigating to /pro/simulate, /pro/reports, and /pro/analyze respectively. Component remains a server component.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all 4 integration flows end-to-end</name>
  <files>no files modified — verification only</files>
  <action>
Present the user with the verification checklist below. All 4 integration flows must be tested in the running application. Task 1 wired the Home CTA buttons; the remaining flows (Simulate -> Reports, Analyze -> PDF, Settings -> context) were built in prior phases (50-53). This checkpoint confirms everything works together.
  </action>
  <what-built>
    Task 1 wired the 3 Home CTA buttons. The remaining flows (Simulate -> Reports, Analyze -> PDF, Settings -> context) were built in prior phases. This checkpoint verifies all 4 integration requirements work correctly together.
  </what-built>
  <how-to-verify>
**INT-01: Home CTAs (newly wired)**
1. Navigate to /pro (Home / Executive Briefing)
2. In the Hamilton's View card, click "Simulate Change" -> should navigate to /pro/simulate
3. Go back to /pro, click "Generate Board Brief" -> should navigate to /pro/reports
4. Go back to /pro, click "Ask Hamilton" -> should navigate to /pro/analyze
5. Also verify RecommendedActionCard links to /pro/simulate with a category param (if thesis data loaded)

**INT-02: Simulate -> Reports flow**
6. Navigate to /pro/simulate
7. Select a fee category with sufficient data (e.g., overdraft)
8. Adjust the slider and run Hamilton interpretation
9. Click "Generate Board Scenario Summary" button
10. Verify navigation to /pro/reports?scenario_id={some-uuid}
11. Verify the Reports page loads with the scenario context available

**INT-03: Analyze -> PDF export**
12. Navigate to /pro/analyze
13. Run an analysis query (any focus tab)
14. Wait for the stream to complete
15. Click "Export PDF" button
16. Verify a PDF file downloads with analysis content

**INT-04: Cross-screen context consistency**
17. Navigate to /pro/settings and note the institution name, type, asset tier, and Fed district
18. Navigate to /pro (Home) — verify institution name appears in briefing context
19. Navigate to /pro/analyze — verify same institution context is referenced
20. Navigate to /pro/simulate — verify same institution context
21. If possible, change institution in Settings and verify the change propagates on next page load
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint — no automated check"</automated>
  </verify>
  <done>All 4 integration flows confirmed working by human verification: Home CTAs navigate correctly, Simulate -> Reports scenario handoff works, Analyze -> PDF exports successfully, and institution context is consistent across all screens.</done>
  <resume-signal>Type "approved" if all 4 flows work correctly, or describe which flow(s) have issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client nav -> Pro routes | Navigation links are internal; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Spoofing | HamiltonViewCard links | accept | Links go to auth-gated Pro routes; layout.tsx enforces canAccessPremium() |
| T-54-02 | Tampering | scenario_id URL param | mitigate | Reports page validates scenario_id belongs to authenticated user (existing WHERE user_id clause) |
| T-54-03 | Info Disclosure | PDF export | accept | PDF contains user's own analysis data; auth checked at /api/pro/report-pdf endpoint |
</threat_model>

<verification>
All 4 integration flows verified:
1. Home CTAs: 3 buttons navigate to correct /pro/* routes
2. Simulate -> Reports: scenario_id passed in URL, Reports page reads it
3. Analyze -> PDF: Export button triggers download
4. Settings -> Context: institution_name, institution_type, asset_tier, fed_district consistent across screens
</verification>

<success_criteria>
- HamiltonViewCard has 3 working Link elements to /pro/simulate, /pro/reports, /pro/analyze
- No dead-end buttons remain on the Home screen (except deferred "Full Dashboard" and "Export PDF" header buttons)
- TypeScript compiles without errors: `npx tsc --noEmit` passes
- Human verifies all 4 integration flows work end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/54-integration-pass/54-01-SUMMARY.md`
</output>
