---
phase: 49-monitor-live-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/pro/(hamilton)/monitor/page.tsx
  - src/lib/hamilton/seed-monitor-data.ts
  - src/components/hamilton/monitor/SignalFeed.tsx
  - src/components/hamilton/monitor/WatchlistPanel.tsx
autonomous: true
requirements: [MON-01, MON-02]
must_haves:
  truths:
    - "Signal feed renders rows from real hamilton_signals table — no demo data injected"
    - "When hamilton_signals is empty, a designed onboarding card appears (not broken layout)"
    - "User can add an institution to watchlist and see it appear immediately"
    - "User can remove an institution from watchlist and see it disappear immediately"
    - "No hardcoded demo data appears anywhere on the Monitor screen"
  artifacts:
    - path: "src/app/pro/(hamilton)/monitor/page.tsx"
      provides: "Monitor page with no seedMonitorData call"
      contains: "fetchMonitorPageData"
    - path: "src/components/hamilton/monitor/SignalFeed.tsx"
      provides: "Designed empty state with onboarding guidance"
      contains: "Add institutions to your watchlist"
    - path: "src/components/hamilton/monitor/WatchlistPanel.tsx"
      provides: "Watchlist panel with no hardcoded FEE_MOVEMENTS"
    - path: "src/lib/hamilton/seed-monitor-data.ts"
      provides: "Dev-only utility guarded from production use"
  key_links:
    - from: "src/app/pro/(hamilton)/monitor/page.tsx"
      to: "src/lib/hamilton/monitor-data.ts"
      via: "fetchMonitorPageData(user.id)"
      pattern: "fetchMonitorPageData"
---

<objective>
Strip all demo/seed data from the Monitor screen and ensure real DB queries drive the signal feed and watchlist. Design a proper empty state for when no signals exist.

Purpose: MON-01 (signal feed from real data with empty state) and MON-02 (watchlist CRUD against real table) require removing the seedMonitorData call that short-circuits real data, replacing the minimal empty state with onboarding guidance, and stripping hardcoded FEE_MOVEMENTS from WatchlistPanel.

Output: Monitor page that renders exclusively from hamilton_signals and hamilton_watchlists tables, with a designed empty state when no data exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-monitor-live-data/49-CONTEXT.md
@.planning/phases/49-monitor-live-data/49-RESEARCH.md

@src/app/pro/(hamilton)/monitor/page.tsx
@src/lib/hamilton/seed-monitor-data.ts
@src/components/hamilton/monitor/SignalFeed.tsx
@src/components/hamilton/monitor/WatchlistPanel.tsx
@src/app/pro/(hamilton)/monitor/actions.ts
@src/lib/hamilton/monitor-data.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/hamilton/monitor-data.ts:
```typescript
export interface WatchlistEntry {
  id: string;
  name: string;
  status: "current" | "review_due" | "unknown";
}

export interface MonitorPageData {
  status: { ...metrics };
  topAlert: TopAlert | null;
  signalFeed: SignalEntry[];
  watchlist: WatchlistEntry[];
}

export async function fetchMonitorPageData(userId: number): Promise<MonitorPageData>;
```

From src/app/pro/(hamilton)/monitor/actions.ts:
```typescript
export async function addToWatchlist(userId: number, institutionId: string): Promise<void>;
export async function removeFromWatchlist(userId: number, institutionId: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip demo data from Monitor page and design empty state</name>
  <files>
    src/app/pro/(hamilton)/monitor/page.tsx
    src/lib/hamilton/seed-monitor-data.ts
    src/components/hamilton/monitor/SignalFeed.tsx
    src/components/hamilton/monitor/WatchlistPanel.tsx
  </files>
  <read_first>
    src/app/pro/(hamilton)/monitor/page.tsx
    src/lib/hamilton/seed-monitor-data.ts
    src/components/hamilton/monitor/SignalFeed.tsx
    src/components/hamilton/monitor/WatchlistPanel.tsx
    src/lib/hamilton/monitor-data.ts
    src/app/pro/(hamilton)/monitor/actions.ts
  </read_first>
  <action>
**monitor/page.tsx** (per D-01, D-03):
- Remove the import of `seedMonitorData` from `@/lib/hamilton/seed-monitor-data`
- Remove line 21: `await seedMonitorData(user.id);`
- Remove the comment "// Ensure demo signal data exists" on line 20
- The page now flows directly: getCurrentUser -> fetchMonitorPageData -> render. No other changes needed.

**seed-monitor-data.ts** (per D-02):
- Add a guard at the top of the `seedMonitorData` function body:
  ```typescript
  if (process.env.NODE_ENV === "production") {
    console.warn("seedMonitorData called in production — skipping");
    return;
  }
  ```
- Add a JSDoc warning: `@deprecated Dev-only utility. Not called from production pages.`
- Do NOT delete the file — it remains useful for dev seeding via CLI or test scripts.

**SignalFeed.tsx** (per D-04, D-05):
- Replace the `EmptyState` function (lines 444-459) with a designed onboarding card:
  - Container: `backgroundColor: "var(--hamilton-surface-container-lowest, #ffffff)"`, `padding: "2.5rem"`, `borderLeft: "4px solid var(--hamilton-outline-variant, #d8c2b8)"`, `display: "flex"`, `flexDirection: "column"`, `gap: "1rem"`, `borderRadius: "0.5rem"`
  - Headline `<h3>`: fontFamily `var(--hamilton-font-serif)`, fontSize `1.5rem`, fontWeight 400, color `var(--hamilton-on-surface)`, text: "No signals yet."
  - Body `<p>`: fontFamily `var(--hamilton-font-sans)`, fontSize `0.9375rem`, color `var(--hamilton-text-secondary)`, lineHeight 1.6, text: "Add institutions to your watchlist to begin monitoring fee movements and competitive shifts."
  - The card must look intentional and designed — not a plain paragraph.

**WatchlistPanel.tsx** (per D-08):
- Remove the entire `FEE_MOVEMENTS` constant array (lines 81-103)
- Remove the JSX section that renders FEE_MOVEMENTS (find the `FEE MOVEMENTS` section heading and its rendering loop)
- Add a `// TODO Phase 50: wire fee movements to real fee_change_events data` comment where the section was
- Keep the WATCHLIST INTEGRITY section and branded card section intact — they render from real data via the `entries` prop
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && grep -c "seedMonitorData" src/app/pro/\(hamilton\)/monitor/page.tsx | grep -q "^0$" && echo "PASS: seedMonitorData removed from page" || echo "FAIL"; grep -q "NODE_ENV.*production" src/lib/hamilton/seed-monitor-data.ts && echo "PASS: production guard added" || echo "FAIL"; grep -q "Add institutions to your watchlist" src/components/hamilton/monitor/SignalFeed.tsx && echo "PASS: empty state has guidance" || echo "FAIL"; grep -c "FEE_MOVEMENTS" src/components/hamilton/monitor/WatchlistPanel.tsx | grep -q "^0$" && echo "PASS: FEE_MOVEMENTS removed" || echo "FAIL"; npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "seedMonitorData" src/app/pro/(hamilton)/monitor/page.tsx returns 0 matches
    - grep "NODE_ENV" src/lib/hamilton/seed-monitor-data.ts returns 1+ matches (production guard)
    - grep "Add institutions to your watchlist" src/components/hamilton/monitor/SignalFeed.tsx returns 1+ matches
    - grep "borderLeft" src/components/hamilton/monitor/SignalFeed.tsx returns 1+ matches (designed card styling)
    - grep "FEE_MOVEMENTS" src/components/hamilton/monitor/WatchlistPanel.tsx returns 0 matches
    - npx tsc --noEmit passes with no errors
    - npx vitest run passes (no regressions)
  </acceptance_criteria>
  <done>
    Monitor page loads data exclusively from fetchMonitorPageData with no demo seed injection. Empty signal feed shows a designed onboarding card. WatchlistPanel has no hardcoded FEE_MOVEMENTS. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server action | Watchlist add/remove mutations cross from client to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Spoofing | addToWatchlist/removeFromWatchlist | mitigate | Already scoped to userId from getCurrentUser() — no user-supplied userId in request body |
| T-49-02 | Tampering | WatchlistPanel entries prop | accept | Data is server-rendered from DB; client cannot tamper with initial state |
</threat_model>

<verification>
1. `grep -c "seedMonitorData" src/app/pro/(hamilton)/monitor/page.tsx` returns `0`
2. `grep "fetchMonitorPageData" src/app/pro/(hamilton)/monitor/page.tsx` returns a match
3. `grep "Add institutions to your watchlist" src/components/hamilton/monitor/SignalFeed.tsx` returns a match
4. `grep -c "FEE_MOVEMENTS" src/components/hamilton/monitor/WatchlistPanel.tsx` returns `0`
5. `npx tsc --noEmit` passes
6. `npx vitest run` passes (no regressions)
</verification>

<success_criteria>
- Monitor page renders signal feed from real hamilton_signals table
- Empty state shows designed onboarding card with guidance text
- No demo/seed data injected on page load
- WatchlistPanel shows only real DB entries, no hardcoded fee movements
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/49-monitor-live-data/49-01-SUMMARY.md`
</output>
