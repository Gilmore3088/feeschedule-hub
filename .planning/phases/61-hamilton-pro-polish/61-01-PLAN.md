---
phase: 61-hamilton-pro-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hamilton/simulate/SimulateWorkspace.tsx
  - src/components/hamilton/reports/ReportWorkspace.tsx
  - src/app/pro/(hamilton)/reports/page.tsx
  - src/app/pro/(hamilton)/settings/page.tsx
  - src/components/hamilton/analyze/AnalyzeWorkspace.tsx
  - src/components/hamilton/home/HamiltonViewCard.tsx
autonomous: true
requirements: [PRO-01]

must_haves:
  truths:
    - "Zero hardcoded demo data strings visible on any Pro screen"
    - "Every empty state has icon + serif title + description + CTA per D-03 pattern"
    - "Empty states explain what config step is needed per D-04"
  artifacts:
    - path: "src/components/hamilton/simulate/SimulateWorkspace.tsx"
      provides: "Fake reference HAM-2024-OD-09 removed"
    - path: "src/components/hamilton/reports/ReportWorkspace.tsx"
      provides: "Hardcoded institutionName and peerSetLabel replaced with props"
    - path: "src/app/pro/(hamilton)/settings/page.tsx"
      provides: "Member since 2026 removed or derived from real data"
    - path: "src/components/hamilton/analyze/AnalyzeWorkspace.tsx"
      provides: "Empty state upgraded to D-03 gold standard pattern"
      contains: "category_title"
    - path: "src/components/hamilton/home/HamiltonViewCard.tsx"
      provides: "Empty state upgraded to D-03 gold standard pattern"
      contains: "hamilton-font-serif"
  key_links:
    - from: "src/app/pro/(hamilton)/reports/page.tsx"
      to: "src/components/hamilton/reports/ReportWorkspace.tsx"
      via: "institutionName and peerSetLabel props threaded from server"
      pattern: "institutionName.*user"
---

<objective>
Strip all hardcoded demo data from Pro screens and upgrade empty states to the D-03 gold standard pattern (icon + serif title + description + CTA).

Purpose: PRO-01 requires zero fake data visible to users. Research identified exactly 3 fake data locations and several minimal empty states that need upgrading.
Output: Clean Pro screens with designed empty states that explain what configuration step is needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-hamilton-pro-polish/61-RESEARCH.md
@.planning/phases/61-hamilton-pro-polish/61-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip 3 fake data instances and wire real data for Reports</name>
  <read_first>
    - src/components/hamilton/simulate/SimulateWorkspace.tsx (line 300 area -- fake HAM-2024-OD-09 reference)
    - src/components/hamilton/reports/ReportWorkspace.tsx (lines 395-410 -- hardcoded institutionName/peerSetLabel)
    - src/app/pro/(hamilton)/reports/page.tsx (server component that passes props to ReportWorkspace)
    - src/app/pro/(hamilton)/settings/page.tsx (line 121 -- hardcoded "Member since 2026")
    - src/lib/auth.ts (User type -- check if created_at is available)
    - .planning/phases/61-hamilton-pro-polish/61-RESEARCH.md (full context on each finding)
  </read_first>
  <files>
    src/components/hamilton/simulate/SimulateWorkspace.tsx,
    src/components/hamilton/reports/ReportWorkspace.tsx,
    src/app/pro/(hamilton)/reports/page.tsx,
    src/app/pro/(hamilton)/settings/page.tsx
  </files>
  <action>
    Three surgical removals per D-01 and D-02:

    1. **SimulateWorkspace.tsx line ~300:** Delete the entire `<p>` element containing `HAM-2024-OD-09` and `Last Live Sync: 12s ago`. The header already shows the fee category title. No replacement needed -- just remove.

    2. **ReportWorkspace.tsx lines ~400-401:** Replace hardcoded `institutionName="Your Institution"` and `peerSetLabel="National Index"` with props threaded from the server page. Update the ReportWorkspace props interface to accept `institutionName: string` and `peerSetLabel: string`. The ConfigSidebar already receives these as props -- the fix is at the callsite in ReportWorkspace, not inside ConfigSidebar.

    3. **reports/page.tsx (server component):** Import `getSavedPeerSets` from `@/lib/crawler-db/saved-peers` (same pattern as settings/page.tsx). Fetch `const peerSets = await getSavedPeerSets(String(user.id)).catch(() => [])`. Pass `institutionName={user.institution_name ?? ""}` and `peerSetLabel={peerSets[0]?.name ?? "National Index"}` to ReportWorkspace. The fallback "National Index" is acceptable since it's the default peer set context (not fake data -- it's the actual default behavior).

    4. **settings/page.tsx line ~121:** Remove the hardcoded `"Member since 2026"` span entirely. Check if the `users` table has a `created_at` column by reading auth.ts User type. If `created_at` is on the User type, derive the year: `Member since ${new Date(user.created_at).getFullYear()}`. If not available, simply remove the line -- do not add a DB migration for this cosmetic detail.
  </action>
  <verify>
    <automated>grep -rn "HAM-2024-OD-09" src/ && echo "FAIL: fake reference still exists" && exit 1 || echo "PASS: no fake reference"; grep -rn '"Your Institution"' src/components/hamilton/reports/ && echo "FAIL: hardcoded institution name" && exit 1 || echo "PASS: no hardcoded institution"; grep -rn '"Member since 2026"' src/ && echo "FAIL: hardcoded member since" && exit 1 || echo "PASS: no hardcoded member since"; npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "HAM-2024-OD-09" src/` returns nothing
    - `grep -rn '"Your Institution"' src/components/hamilton/reports/` returns nothing
    - `grep -rn '"Member since 2026"' src/` returns nothing
    - `grep -q "institutionName" src/app/pro/\(hamilton\)/reports/page.tsx` passes (prop is threaded)
    - `npx vitest run` passes (no regressions)
  </acceptance_criteria>
  <done>All 3 fake data instances removed. ReportWorkspace receives real institution name and peer set label from the server page via props. Settings page no longer shows hardcoded membership date.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade empty states to D-03 gold standard pattern</name>
  <read_first>
    - src/components/hamilton/monitor/SignalFeed.tsx (lines 444-522 -- gold standard empty state pattern with exact design tokens)
    - src/components/hamilton/analyze/AnalyzeWorkspace.tsx (lines 276-286 -- current minimal empty state)
    - src/components/hamilton/simulate/SimulateWorkspace.tsx (lines 395-399 -- current bare empty state)
    - src/components/hamilton/home/HamiltonViewCard.tsx (line 108 area -- current minimal text-only empty state)
    - .planning/phases/61-hamilton-pro-polish/61-RESEARCH.md (Empty State Patterns section + Code Examples section for template)
  </read_first>
  <files>
    src/components/hamilton/analyze/AnalyzeWorkspace.tsx,
    src/components/hamilton/simulate/SimulateWorkspace.tsx,
    src/components/hamilton/home/HamiltonViewCard.tsx
  </files>
  <action>
    Upgrade empty states on Analyze, Simulate, and HamiltonViewCard to match the SignalFeed gold standard (D-03 + D-04). Home screen components WhatChangedCard, PriorityAlertsCard, and MonitorFeedPreview already have acceptable empty states with text + inline links per research -- no upgrade needed for those.

    Use the exact design tokens from SignalFeed lines 444-522 (documented in RESEARCH.md "Gold Standard Pattern" section):
    - Container: `var(--hamilton-surface-container-lowest)` bg, `4px` left border with `var(--hamilton-outline-variant)`
    - Icon circle: `3rem` circle with `var(--hamilton-surface-container-high)` bg, lucide icon in `var(--hamilton-primary)` stroke
    - Headline: `var(--hamilton-font-serif)`, italic, `1.25rem`, `var(--hamilton-on-surface)`
    - Body: `var(--hamilton-font-sans)`, `0.875rem`, `var(--hamilton-text-secondary)`, line-height 1.6
    - CTA: `burnished-cta` class (already in globals.css), arrow-right lucide icon

    **AnalyzeWorkspace.tsx (lines ~276-286):** Replace the current minimal `<div className="text-center py-16">` empty state with the gold standard pattern. Use `MessageSquare` lucide icon. Headline: "Begin Your Analysis". Body: "Ask Hamilton to analyze any fee category, competitive position, or market trend. Select a focus area above to guide the conversation." (per D-04 -- references what to do). CTA: "Start Analysis" button that focuses or scrolls to the analysis input bar at the bottom of the screen. Import `ArrowRight` from lucide-react for the CTA icon.

    **SimulateWorkspace.tsx (lines ~395-399):** Replace "Select a fee category to begin simulation." with the gold standard pattern. Use `SlidersHorizontal` lucide icon. Headline: "Configure Your Scenario". Body: "Select a fee category from the sidebar to model pricing changes and see competitive positioning impacts." (per D-04 -- tells user what config step is needed). No CTA needed since the category selector is already visible in the sidebar.

    **HamiltonViewCard.tsx (line ~108 area):** Upgrade the current minimal text-only fallback ("AI analysis temporarily unavailable. Positioning data below is current.") to the gold standard pattern. Use `BarChart3` lucide icon. Headline: "Positioning Analysis Loading". Body: "Hamilton will display your institution's competitive positioning once analysis data is available. Check Settings to ensure your institution is configured." No CTA needed -- the text references the configuration step per D-04.

    Import lucide icons at top of each file: `import { MessageSquare, ArrowRight } from "lucide-react"` for Analyze, `import { SlidersHorizontal } from "lucide-react"` for Simulate, `import { BarChart3 } from "lucide-react"` for HamiltonViewCard.
  </action>
  <verify>
    <automated>grep -q "hamilton-font-serif" src/components/hamilton/analyze/AnalyzeWorkspace.tsx && echo "PASS: Analyze has serif headline" || echo "FAIL"; grep -q "hamilton-font-serif" src/components/hamilton/simulate/SimulateWorkspace.tsx && echo "PASS: Simulate has serif headline" || echo "FAIL"; grep -q "hamilton-font-serif" src/components/hamilton/home/HamiltonViewCard.tsx && echo "PASS: HamiltonViewCard has serif headline" || echo "FAIL"; grep -q "burnished-cta\|hamilton-outline-variant" src/components/hamilton/analyze/AnalyzeWorkspace.tsx && echo "PASS: Analyze uses design tokens" || echo "FAIL"; grep -qE "Start Analysis|View Examples|Try a Question" src/components/hamilton/analyze/AnalyzeWorkspace.tsx && echo "PASS: Analyze has concrete CTA" || echo "FAIL"; npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "hamilton-font-serif" src/components/hamilton/analyze/AnalyzeWorkspace.tsx` passes
    - `grep -q "hamilton-font-serif" src/components/hamilton/simulate/SimulateWorkspace.tsx` passes
    - `grep -q "hamilton-font-serif" src/components/hamilton/home/HamiltonViewCard.tsx` passes
    - `grep -q "hamilton-outline-variant" src/components/hamilton/analyze/AnalyzeWorkspace.tsx` passes
    - `grep -q "hamilton-outline-variant" src/components/hamilton/simulate/SimulateWorkspace.tsx` passes
    - `grep -qE "Start Analysis|View Examples|Try a Question" src/components/hamilton/analyze/AnalyzeWorkspace.tsx` passes (concrete CTA exists)
    - All empty states have: icon element, serif italic headline, descriptive body text, design tokens matching SignalFeed
    - `npx vitest run` passes
  </acceptance_criteria>
  <done>Analyze, Simulate, and HamiltonViewCard empty states match the SignalFeed gold standard: icon circle + serif italic headline + descriptive body explaining what to do + Hamilton design tokens. Per D-03 and D-04. Home screen components WhatChangedCard, PriorityAlertsCard, and MonitorFeedPreview confirmed acceptable as-is per research.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server page to client component | Props passed from server (user data) to client ReportWorkspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Information Disclosure | reports/page.tsx | mitigate | Only pass institution_name and peer set name (not IDs, tokens, or sensitive user fields) as props to client component |
| T-61-02 | Spoofing | ReportWorkspace props | accept | Props are server-rendered, not user-modifiable; institution_name comes from authenticated user record |
</threat_model>

<verification>
- `grep -rn "HAM-2024-OD-09\|Your Institution\|Member since 2026" src/` returns zero matches
- Empty states on Analyze, Simulate, and HamiltonViewCard use `hamilton-font-serif` and `hamilton-outline-variant` design tokens
- Analyze empty state has a concrete CTA ("Start Analysis" or similar)
- `npx vitest run` passes with no regressions
</verification>

<success_criteria>
- Zero hardcoded demo data strings remain in any Pro screen file
- ReportWorkspace receives institution name and peer set label from real user data
- Analyze, Simulate, and HamiltonViewCard have designed empty states matching SignalFeed gold standard (icon + serif title + description)
- Analyze empty state includes a concrete CTA button
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/61-hamilton-pro-polish/61-01-SUMMARY.md`
</output>
