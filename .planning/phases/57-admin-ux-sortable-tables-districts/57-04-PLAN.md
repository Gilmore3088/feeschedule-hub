---
phase: 57-admin-ux-sortable-tables-districts
plan: 04
type: execute
wave: 2
depends_on:
  - 57-01
  - 57-02
  - 57-03
files_modified:
  - src/app/admin/page.tsx
  - src/app/admin/market/page.tsx
  - src/app/admin/districts/[id]/page.tsx
  - src/app/admin/districts/page.tsx
  - src/app/admin/review/page.tsx
  - src/app/admin/fees/catalog/page.tsx
  - src/app/admin/fees/catalog/[category]/page.tsx
  - src/app/admin/institutions/page.tsx
  - src/app/admin/pipeline/page.tsx
  - src/app/admin/research/articles/page.tsx
  - src/app/admin/hamilton/leads/leads-table.tsx
autonomous: false
requirements:
  - ADM-04

must_haves:
  truths:
    - "No admin page has horizontal overflow at 768px viewport width"
    - "Wide tables have overflow-x-auto containers for horizontal scroll on tablet"
    - "Dashboard and districts pages have adaptive layouts (stacked on mobile)"
    - "Simpler pages have overflow-x-auto wrapping as minimum treatment"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Dashboard with responsive grid"
      contains: "overflow-x-auto"
  key_links: []
---

<objective>
Ensure all admin pages render without horizontal overflow at 768px tablet viewport. Complex pages (dashboard, districts, market) get full adaptive layouts; simpler pages get overflow-x-auto wrapping on wide tables (per D-13, D-14, D-15).

Purpose: Admin UX is usable on tablet screens without horizontal page overflow.
Output: Responsive-audited admin pages with overflow handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-admin-ux-sortable-tables-districts/57-CONTEXT.md
@.planning/phases/57-admin-ux-sortable-tables-districts/57-RESEARCH.md

<interfaces>
From src/app/admin/layout.tsx:
```typescript
// Sidebar: hidden below md: breakpoint (already responsive)
<aside className="hidden md:flex flex-col w-[180px] shrink-0 ...">
// Main: min-w-0 prevents overflow, flex-1 fills space
<main className="admin-content flex-1 min-w-0 px-5 py-5 lg:px-7">
  <div className="mx-auto max-w-[1600px]">{children}</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix responsive layout across all admin pages</name>
  <files>
    src/app/admin/page.tsx,
    src/app/admin/market/page.tsx,
    src/app/admin/districts/[id]/page.tsx,
    src/app/admin/districts/page.tsx,
    src/app/admin/review/page.tsx,
    src/app/admin/fees/catalog/page.tsx,
    src/app/admin/fees/catalog/[category]/page.tsx,
    src/app/admin/institutions/page.tsx,
    src/app/admin/pipeline/page.tsx,
    src/app/admin/research/articles/page.tsx,
    src/app/admin/hamilton/leads/leads-table.tsx
  </files>
  <read_first>
    - src/app/admin/page.tsx (dashboard -- complex multi-column layout, most likely to overflow)
    - src/app/admin/market/page.tsx (two-column Bloomberg layout, col-span-8/col-span-4)
    - src/app/admin/review/page.tsx (table with many columns)
    - src/app/admin/fees/catalog/page.tsx (table page)
    - src/app/admin/fees/catalog/[category]/page.tsx (category detail with institution table)
    - src/app/admin/institutions/page.tsx (large table)
    - src/app/admin/pipeline/page.tsx (tab-based with sub-tables)
    - src/app/admin/layout.tsx (confirm sidebar already responsive)
  </read_first>
  <action>
Per D-13, D-14, D-15: Apply responsive fixes across all admin pages. Use Claude's discretion (D-15) to triage:

**Full adaptive treatment (complex/high-value pages):**

1. **Dashboard (`src/app/admin/page.tsx`):**
   - Multi-column grids (e.g., `grid-cols-4`, `grid-cols-2`) should collapse to single column on mobile: add `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` pattern.
   - Two-column layouts (e.g., `grid-cols-12` with `col-span-8` + `col-span-4`) collapse to stacked: `grid-cols-1 lg:grid-cols-12`.
   - Any fixed-width elements that cause overflow: add `min-w-0` or `overflow-hidden`.

2. **Market page (`src/app/admin/market/page.tsx`):**
   - Two-column layout (`col-span-8` + `col-span-4`) collapses to stacked on tablet: `grid-cols-1 lg:grid-cols-12`.
   - Hero cards (4 fixed) collapse to 2-column on tablet, 1 on mobile: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`.

3. **District detail (`src/app/admin/districts/[id]/page.tsx`):**
   - Indicator grid collapses: `grid-cols-1 sm:grid-cols-2` (already has md:grid-cols-2).
   - Theme cards grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` (currently `grid-cols-2 md:grid-cols-4`).

**Overflow-x-auto treatment (table-heavy pages):**

For each of these pages, ensure every `<table>` or `SortableTable` or `ServerSortableTable` is wrapped in a container with `overflow-x-auto`:

4. **Review page:** ServerSortableTable already includes `overflow-x-auto` internally (built into the component in Plan 02). Verify no additional outer constraints prevent scroll.

5. **Fees catalog:** Same treatment. ServerSortableTable includes `overflow-x-auto`.

6. **Category detail (`fees/catalog/[category]/page.tsx`):** Check institution-table.tsx for overflow handling.

7. **Institutions page:** Large table -- wrap in `overflow-x-auto` if not already.

8. **Pipeline page:** Check sub-tables (coverage-table, data-sources-status) for overflow wrappers.

9. **Research/articles and hamilton/leads:** Wrap tables in `overflow-x-auto` if not already.

**General rules applied everywhere:**
- No `min-w-[...]` that forces overflow at 768px.
- No `whitespace-nowrap` on containers without `overflow-x-auto`.
- Fixed-width columns (`w-[200px]`) get `min-w-0` or become `max-w-[200px]`.
- Text that might overflow gets `truncate` or `line-clamp-1`.
  </action>
  <verify>
    <automated>cd /Users/jgmbp/Desktop/feeschedule-hub && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "overflow-x-auto" src/app/admin/page.tsx OR the dashboard uses SortableTable (which has it built in)
    - grep -q "lg:grid-cols-12" src/app/admin/market/page.tsx (two-column collapses on mobile)
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>All admin pages render without horizontal overflow at 768px. Dashboard and market pages have adaptive grid collapse. All table pages have overflow-x-auto containers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of responsive layouts</name>
  <files>N/A</files>
  <action>
Human visually verifies the responsive layouts across all admin pages at 768px and 375px viewport widths. This is a visual verification checkpoint -- no code changes are made in this task. The executor presents the verification steps and waits for user feedback.
  </action>
  <what-built>
    Responsive admin layouts: sortable tables across all pages, page-size selectors on review/catalog, tabbed district detail with full intelligence, overflow handling at tablet breakpoint.
  </what-built>
  <how-to-verify>
    1. Open browser devtools, set viewport to 768px width.
    2. Visit /admin (dashboard) -- confirm no horizontal overflow, grids stack appropriately.
    3. Visit /admin/market -- confirm two-column layout stacks on tablet.
    4. Visit /admin/districts -- confirm sortable table renders, columns visible.
    5. Visit /admin/districts/1 -- confirm four tabs work, content switches.
    6. Visit /admin/review -- confirm page-size selector (25/50/100) appears, change to 100.
    7. Visit /admin/fees/catalog -- confirm table is sortable, page-size selector works.
    8. Click any table column header -- confirm sort arrow changes, URL updates.
    9. Reload page -- confirm sort state persists from URL.
    10. Set viewport to 375px (mobile) -- confirm no horizontal page overflow on any page above.
  </how-to-verify>
  <verify>
    <automated>echo "Manual visual verification -- user confirms via resume-signal"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms "approved" or provides specific issues to address
    - No horizontal page overflow at 768px viewport width on any admin page
  </acceptance_criteria>
  <done>User has visually verified all admin pages render correctly at tablet (768px) and mobile (375px) viewport widths with no horizontal overflow.</done>
  <resume-signal>Type "approved" or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure CSS/layout changes, no new data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-06 | Information Disclosure | Responsive layout | accept | No data is hidden/exposed differently based on viewport -- horizontal scroll preserves all columns. No truncation of sensitive data. |
</threat_model>

<verification>
1. Open devtools at 768px viewport -- no horizontal overflow on any admin page
2. Dashboard grids collapse to fewer columns on tablet
3. Market two-column layout stacks
4. All tables scroll horizontally within their container (not the page)
5. TypeScript compiles: `npx tsc --noEmit`
6. Tests pass: `npx vitest run src/lib`
</verification>

<success_criteria>
Every admin page renders without horizontal page overflow at 768px viewport width. Complex pages (dashboard, market, districts) have adaptive grid layouts. Table pages have overflow-x-auto containers. Human verification confirms usable tablet experience.
</success_criteria>

<output>
After completion, create `.planning/phases/57-admin-ux-sortable-tables-districts/57-04-SUMMARY.md`
</output>
