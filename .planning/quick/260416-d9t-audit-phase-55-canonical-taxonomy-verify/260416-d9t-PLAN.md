---
id: 260416-d9t
title: Audit Phase 55 canonical taxonomy — verify column, backfill, NEVER_MERGE, map size
mode: quick
status: complete
---

# Plan: Audit Phase 55 Canonical Taxonomy

## Objective

Verify each Phase 55 success criterion against the current codebase and runtime state. Output a gap list that scopes the remaining Phase 55 work.

Phase 55 is marked "not started" on ROADMAP.md, but preliminary grepping shows substantial infrastructure already shipped inside Phases 56–59. This audit determines what is actually unfinished.

## Scope

Read-only investigation. No code changes. Deliverable is `SUMMARY.md` containing the gap list.

## Success Criteria under Audit

1. `canonical_fee_key` column present on `extracted_fees`, idempotent migration
2. ~200-key canonical map in `fee_analysis.py`; unit test asserts count == `fee-taxonomy.ts`
3. Every `extracted_fees` row has non-null `canonical_fee_key` post-backfill; index counts identical pre/post
4. pytest test asserting NEVER_MERGE pairs (NSF/overdraft, domestic/intl wires) runs in CI before alias expansion ships
5. Roomba agent flags 3+ stdev outliers with `flagged` review_status; visible in admin

## Method

- Grep codebase for column DDL, map size, backfill script, NEVER_MERGE test, Roomba command
- Inspect CI workflow for pytest marker filter (is the NEVER_MERGE test actually run?)
- Query local SQLite schema (prod Postgres unreachable — stale credentials in `.env`)
- Compile findings into a single gap list with concrete follow-up tasks

## Out of Scope

- Running the backfill or Roomba in production
- Refreshing DATABASE_URL
- Writing the fix-plans (that is the job of `/gsd-plan-phase 55`)
